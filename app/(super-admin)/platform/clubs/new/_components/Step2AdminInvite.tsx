"use client";

export function Step2AdminInvite() {
  return (
    <div data-testid="step-2-admin-invite" className="text-sm text-muted-foreground">
      Step 2 — Admin invite (shell). Email field lands in the next commit.
    </div>
  );
}
