"use client";

export function Step4Players() {
  return (
    <div data-testid="step-4-players" className="text-sm text-muted-foreground">
      Step 4 — Initial players (shell). Single form + CSV upload land in later commits.
    </div>
  );
}
