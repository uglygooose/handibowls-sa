import { TabStub } from "./TabStub";

export function DrawTab() {
  return (
    <TabStub
      title="Draw"
      blurb="Bracket tree with live match cards, BYE chips, feeder lines, and the per-round status meta. Click a match to score it."
      phase="7c-ii"
    />
  );
}
