import { TabStub } from "./TabStub";

export function ScoringTab() {
  return (
    <TabStub
      title="Scoring"
      blurb="Bulk-entry grid with arrow-key cell nav, Enter to save row, Shift+Enter to save and advance, Esc to cancel."
      phase="7c-iii"
    />
  );
}
