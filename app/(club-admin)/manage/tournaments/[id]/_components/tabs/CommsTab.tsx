import { TabStub } from "./TabStub";

export function CommsTab() {
  return (
    <TabStub
      title="Comms"
      blurb="Compose announcements to entrants. In-app + email channels only — SMS is locked out per Q6."
      phase="7c-iv"
    />
  );
}
