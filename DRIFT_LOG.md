# HandiBowls — Drift Log

Single source of truth for every piece of drift between Claude Design output / rebuild-plan intent and shipped code. Everything here is **deliberately deferred** — either to Phase 12.5 (design fidelity polish) or to a named later phase. Nothing here is "maybe one day"; every entry has an owner-phase.

**Rules:**
- Every new drift item gets logged here in the same commit it's discovered in. If it can't be logged, it can't be deferred — fix it inline.
- Append-only during a phase. When a drift item is fixed, change `- [ ]` to `- [x]`, wrap the text in `~~strike-through~~`, and append `Closed: <phase> <SHA>`. Never delete entries.
- Claude Code owns updates. Human reviews at phase-end.
- Open count: `grep -c "^- \[ \]" DRIFT_LOG.md`
- Closed count: `grep -c "^- \[x\]" DRIFT_LOG.md`
- Every stop-and-report includes: `Drift log: N open (M new this phase, K closed this phase).`

---

## Phase 4c.6 — blocking (must close before 4d)

- [x] ~~**`@hookform/resolvers@3.10.0` incompatible with Zod 4.** `zodResolver` checks `Array.isArray(error?.errors)` but Zod 4 renamed `ZodError.errors` → `ZodError.issues`. Every `form.trigger()` call throws and every form submit rejects silently; the bug was masked in 4c by the auth-timing redirect. Fix: bump to `@hookform/resolvers@^5` (or latest Zod 4-compatible major), rerun all form-bearing suites (signup, login, invite accept, all 5 wizard steps), verify E2Es green. Discovered: Phase 4c.5 diagnosis. Owning phase: 4c.6.~~ Closed: 4c.6 (`84ed0d4`), 2026-04-23.
- [x] ~~**createClub RPC statement_timeout + dev-invite-banner gate + draft back-compat shim.** Three layered issues unmasked by fixing each in turn. (1) `create_club_with_dependencies` (migration 014) has no function-level `statement_timeout` and inherits `authenticated`'s 5s role-level default; over PostgREST HTTP the RPC tripped SQLSTATE 57014 ("canceling statement due to statement timeout") on Windows WSL2 cold-invocation, while 40/40 integration tests passed via direct psql (as postgres/supabase_admin, uncapped). Migration 016 hoists a per-function `set statement_timeout to '30s'` on the function definition (PostgREST 12.2+ pattern — scoped per-RPC, doesn't weaken `authenticated`'s global ceiling). (2) Unmasked by (1): the E2E reached the club detail page for the first time and exposed `isDevBannerEnabled()` returning false under Playwright's prod-build server because `next build` inlines `NODE_ENV === "production"` into client bundles. Gate rewritten to be production-opt-out via `NEXT_PUBLIC_APP_ENV` (prod deploys must set `NEXT_PUBLIC_APP_ENV=production`); `.env.test` adds `NEXT_PUBLIC_APP_ENV=test` as an explicit E2E opt-in. (3) Strict-parse cleanup in `_draft.ts`: removed the pre-TTL legacy-payload branch (per CLAUDE.md "don't use backwards-compat shims when you can just change the code"; pre-launch, zero real drafts to preserve). Discovered: Phase 4c.6 E2E unblock, 2026-04-24.~~ Closed: 4c.6 (`15cdf0f`), 2026-04-24.

---

## Phase 12.5 — Design fidelity polish (primary target)

### Landing surface — `app/page.tsx` + `app/(marketing)/_sections/`

- [x] ~~**T20 compass card design fidelity.** Three sub-bullets on the same surface (consolidated from three separate entries at Phase 12 triage, 2026-04-30):
  - **Wedge labels missing.** Design shows `N / NE / E / SE / S / SW / W / NW` + `A / B / C / D` grades inside each wedge. Shipped has blank wedges with only N/E/S/W on the outer ring.
  - **Grade legend wording drifted.** Design: `A · On the jack / B · In zone / C · Off zone / D · No bowl`. Shipped: `A — dead weight to the jack / B — hugs the zone / C — in the head / D — off the rink`. BSA-aligned wording lost.
  - **Metadata drifted.** Design: `BSA T20 · DRAW SHOT`, `END 4 OF 20`, `82%` running percentage. Shipped: `STATION 3 · DRAW TO JACK`, `END 4 OF 6`, no percentage.
  Owner: Phase 12.5 design fidelity sweep (12-6). Single fix session covers all three sub-bullets. Discovered: Phase 4 prep, 2026-04-23.~~ Closed: Phase 12 / 12-6 (`92543b6`), 2026-04-30. `app/(marketing)/_sections/ShowcaseT20.tsx` rewritten against design source `landing.jsx:273-368`. All 8 wedges now render with N/NE/E/SE/S/SW/W/NW labels + A/B/C/D grade letters; wedge angle convention shifted by -22.5° so wedge 0 (N) sits straight up matching the design; filled wedge moved to SE (i=3, grade A) aligning with the landing-marker bowl at (248, 232). Grade legend strings restored to BSA-aligned wording. Card header rewritten to "BSA T20 · Draw shot" + "End 4 of 20" + "82%" badge; the placeholder player name "Nthabi Mokoena" + the standalone big "A" grade letter + the footer "End 4 of 6 / Card locked" are dropped (none in design source).
- [x] ~~**T20 compass card — grade legend wording drifted.** Design: `A · On the jack / B · In zone / C · Off zone / D · No bowl`. Shipped: `A — dead weight to the jack / B — hugs the zone / C — in the head / D — off the rink`. BSA-aligned wording lost. Discovered: Phase 4 prep, 2026-04-23.~~ Closed: Phase 12 triage (`9f77b73`), 2026-04-30 — consolidated into the T20 compass card design fidelity entry above.
- [x] ~~**T20 compass card — metadata drifted.** Design: `BSA T20 · DRAW SHOT`, `END 4 OF 20`, `82%` running percentage. Shipped: `STATION 3 · DRAW TO JACK`, `END 4 OF 6`, no percentage. Discovered: Phase 4 prep, 2026-04-23.~~ Closed: Phase 12 triage (`9f77b73`), 2026-04-30 — consolidated into the T20 compass card design fidelity entry above.
- [x] ~~**Top nav system — verify deliberate-or-drift.** Design source: pill-style `LANDING / LOGIN / SIGNUP / INVITE`, teal `BOWLS` wordmark. Shipped: text links `Product / Tournaments / T20 / Clubs / About`, all-black wordmark. **Verification needed before classifying:** confirm with the original Claude Design source whether the shipped text-links nav was a deliberate v1 redesign during marketing surface work (in which case → reclassify as Decision and close), or a deferred drift (in which case → fold into Phase 12.5 design fidelity sweep, NICE → 12-6). Owner: Phase 12 verification step (inline at 12-6 open). Discovered: Phase 4 prep, 2026-04-23; reworded Phase 12 triage, 2026-04-30.~~ Closed: Phase 12 / 12-6 triage, 2026-04-30 — DROP. Verified deliberate v1 marketing-pass change, not drift. Original design's `LANDING / LOGIN / SIGNUP / INVITE` pill nav was a dev-time page-switcher (a Claude Design tool affordance for jumping between mockup states), not the real v1 marketing nav. The shipped `Product / Tournaments / T20 / Clubs / About` text-link nav is the real production state.
- [x] ~~**Hero heading wording — verify deliberate-or-drift.** Design source: `TAP WHERE IT FINISHED. ZONES, GRADES, PERCENTAGES — HANDLED.` Shipped: `EIGHT ZONES. ONE JACK. PROOF YOU'RE IMPROVING.` **Verification needed before classifying:** as the Top nav entry above. If deliberate → reclassify as Decision; if drift → fold into Phase 12.5 design fidelity sweep (NICE → 12-6). Owner: Phase 12 verification step (inline at 12-6 open, paired with Top nav). Discovered: Phase 4 prep, 2026-04-23; reworded Phase 12 triage, 2026-04-30.~~ Closed: Phase 12 / 12-6 triage, 2026-04-30 — DROP. Verified deliberate v1 marketing-pass change, not drift. The shipped headline copy (`TOURNAMENTS, SCORES, AND SKILLS IN YOUR POCKET.`) is the real production state — a brand-led rewrite of the design source's tap-here-it-finished line.

### Auth surfaces — `app/(auth)/`

- [x] ~~**Checkbox arbitrary variant.** Uses `peer-checked:[&>svg]:opacity-100` — non-standard. Replace with shadcn primitive. File: `app/(auth)/_components/Checkbox.tsx`. Discovered: Phase 3 follow-up.~~ Closed: Phase 12 / 12-6 (`3e4720f`), 2026-04-30. `app/(auth)/_components/Checkbox.tsx` rewritten to wrap `@/components/ui/checkbox`'s shadcn Checkbox (Radix Checkbox under the hood). Visual contract preserved: 2px black border, primary-500 fill on check, danger-500 border on `error`, primary-500/30 focus ring. Form-submit semantics unchanged (Radix's hidden form-associated input handles `name` / `required` / `defaultChecked`). Three callers (login remember-me, signup terms, invite code-of-conduct) need no changes. New `tests/app/auth/auth-checkbox.test.tsx` (3 cases) pins the swap.
- [x] ~~**SpeckleField per-instance seed missing.** `patternId` lacks per-instance seed; two adjacent cards with same preset share a pattern. Not currently triggered. File: `components/brand/SpeckleField.tsx`. Owner: Phase 12.5 → 12.5-2 (audit id `speckle-seed`). Acceptance: deterministic per-instance seed via `seedKey` prop on SpeckleField (required for non-fluid renders, document in the type per 12.5-prep locked decision); verify two adjacent same-preset cards render distinct splatter. Discovered: Phase 3 follow-up.~~ Closed: Phase 12.5 / 12.5-2 (`6ad2b17`), 2026-05-01. New `seedKey?: string` prop on `SpeckleField` folds into the SVG pattern id (`speckle-field-${preset}-${density}-${opacityScale}-${seedKey ?? "default"}`). Two adjacent same-preset fields with distinct seedKeys now render distinct dot fields. Non-fluid (explicit-pixel-dimension) renders without seedKey log a dev `console.warn` per the locked decision; fluid (`%`) renders are tolerant of missing seedKey. All 7 production call sites + 2 dev DesignShowcase calls migrated in the same commit (per the user's "don't ship the prop without consumer migrations" guardrail). Pinned by 11 cases in `tests/components/brand/SpeckleField.test.tsx`.
- [x] ~~**Hero pocket highlight — ~2% hex drift.** `-skew-x-[8deg]` + rgba approximation vs Design preview. Non-blocking. Owner: Phase 12.5. Acceptance: pixel-match the design source's hero pocket highlight hex value. Discovered: Phase 3 follow-up.~~ Closed: Phase 12 / 12-6 audit, 2026-04-30 — already at parity. Audit at 12-6: design source's CSS in `landing.jsx`'s standalone HTML uses `.hero__pocket::after { background: var(--primary-500); opacity: .14; transform: skew(-8deg); }`. Shipped `app/(marketing)/_sections/Hero.tsx:124` uses `bg-primary-500 opacity-[0.14] -skew-x-[8deg]` with the same Tailwind→CSS-var resolution. Same colour token, same opacity, same skew. Drift was logged before alignment landed in an earlier commit; entry was stale. No code change required.

### Admin chrome (Phase 7 output, added when shipped)

- [ ] **Hydration mismatch on TournamentsList with junk URL params.** Server-rendered HTML vs client first-render diverges when allow-list filter rewrites URL params during initial render. Page works correctly; warning is cosmetic. Likely needs <Suspense> around useSearchParams or stable URL parsing. Owning phase: Phase 13. Discovered: Phase 7 manual QA, 2026-04-29.
- [ ] **`profile_id` vs `auth.users.id` confusion in queries joining auth.users.** Has now bitten twice — Finding 3 (host-club lookup queried wrong table) and Finding 4 diagnosis (cloud SELECT example used `user_id` not `profile_id` in `club_admin_assignments`). Audit every query that joins or filters on `auth.users` to confirm domain FKs are named correctly. Owning phase: Phase 13. Discovered: Phase 7 manual QA, 2026-04-29.
- [x] ~~**Phase 7 Lighthouse score (`/manage/tournaments/[id]` 86/100/96/91) was measured against a 404 page, not the real detail page.** The `matches:matches(count, status)` embed returned PostgREST 400 from day one (Phase 7c-i landing commit `98d4448`). Finding 4 fix landed during Phase 7 follow-up; the Lighthouse re-run was never done. Owner moves to Phase 12.5 final-polish: run `scripts/lighthouse-tournament-detail.mjs` against the current branch's working detail page, record scores, close. Acceptance: scores recorded in PHASE_LOG / drift log; if any category is below the Phase 13 bar, fold into the player-perf entry above. Owner: Phase 12.5. Discovered: Phase 7 manual QA, 2026-04-29; re-owned Phase 8g, 2026-04-29.~~ Closed: Phase 12 / 12-5, 2026-05-01. Re-ran `scripts/lighthouse-tournament-detail.mjs` against the working detail page (real `/manage/tournaments/[id]` URL resolved at runtime). Scores: **Performance 88, Accessibility 85, Best Practices 100, SEO 91** (desktop preset, single run via WSL2). The original 86/100/96/91 was 404-page noise; the real detail-page numbers are slightly better on Performance + Accessibility but Performance lands just under the ≥90 Phase 13 bar (88) and Accessibility lands at 85. Performance gap: WSL Lighthouse single-run runs are noisy (see `[L67-followup]` Phase 13 entry below); a ≥90 confirmation needs human-driven Lighthouse on a real device. Accessibility 85: needs separate audit pass (likely missing landmark/heading rhythm + button/link aria — folded into the Phase 13 audit task below alongside the sub-90 player perf entry). Recorded in PHASE_LOG.md 12-5 close.
- [ ] **PostgREST embed audit across `_data.ts` files.** Phase 7 admin scorecard match-consuming tabs (Draw / Scoring / Rinks) had a broken `rink:rinks(name)` embed introduced at Phase 7c-i (commit `98d4448` adjacent), masked by the Phase 7 Finding 4 retrospective which fixed an adjacent `matches:matches(count, status)` embed but not this one. Latent until Phase 8 manual QA exposed the same bug across three surfaces (Findings 10, 11, and the Phase 7 admin site). Resolved alongside Phase 8d Findings 10 + 11 in a single atomic commit by extracting `formatRinkLabel` to `lib/format/rink.ts`. Owner: **Phase 13 audit** — sweep all PostgREST embeds in every `_data.ts` and verify every column reference matches the actual schema. Two patterns now repeated (multi-projection aggregate; non-existent column reference) — the audit should grep `\.select\(` for nested `(`, list each embed, and cross-check against `types/database.types.ts`. Discovered: Phase 8d manual QA, 2026-04-29.
- [ ] **Tournament detail RPC consolidation.** Multi-embed PostgREST queries (count + status grouping) are fragile and have already broken once. When player scorecard / T20 / other detail pages need similar shapes, batch them into RPCs returning aggregated row + counts in one round-trip (e.g. `tournament_summary(uuid)`). Owning phase: Phase 13. Discovered: Phase 7 manual QA, 2026-04-29; re-owned Phase 12 triage, 2026-04-30 (defer alongside the Phase 13 PostgREST embed audit).

### Player surfaces (Phase 8 output, added when shipped)

- [ ] **Mocked-Supabase action tests bypass RLS — class-of-bug.** Action-layer tests under `tests/lib/tournaments/club-admin-actions.test.ts` (and similar) mock the Supabase chain at the `from(...).update(...)` level. The mock accepts the call and returns success regardless of what RLS would actually do in the real DB. This bit Phase 8d Diagnostic 1: `submitMatch` was silently no-opping in production while its action test passed (mocked UPDATE returned ok). Migration 028 fixed the symptom (added participant UPDATE policy), but the diagnostic bug-class remains: any future protected mutation can ship with a passing mocked test and a broken real path. Owner: **Phase 13 audit** — sweep all action-layer tests, ensure each protected mutation has at least one real-RLS integration test (test client signed in as the role under test) covering deny + allow paths. Pattern established by `tests/rls/matches.test.ts` migration-028 cases. Discovered: Phase 8d Diagnostic 1, 2026-04-29.
- [x] ~~**Phase 8c scoring grid drifted from design source.**~~ Phase 8c shipped Win 1/2/3 + Lose 1/2/3 buttons (6 added, not in `handibowls/project/player-pages.jsx:PageScorecard`) and missed Mark peel + Skip (2 design-spec items dropped). Closed: Phase 8d follow-up, 2026-04-29 — Win/Lose removed; Mark peel + Skip added matching the design source's outline-button treatment + eye/X icons. Component contract pinned by `tests/app/player/scorecard-live-controls.test.tsx`. **Precedent for Phase 12.5 audit:** sweep all surfaces against `handibowls/project/*.jsx` design sources, log every delta. This entry is the template.
- [ ] **Mark peel vs Skip differentiation in `match_ends`.** The Phase 8d alignment shipped both buttons writing identical 0/0 rows — peel and skip are functionally indistinguishable in the DB. Domain-correct semantics: peel = drawn end (both teams played, neither closer to jack); skip = end abandoned / not played (rare: weather, rink moved, disputed bowl). The design source's buttons are UI-only stubs without onClick contracts so this isn't strictly drift FROM design, but it is a domain gap. Phase 12 polish: thread a `notes` value (`'peel'` / `'skipped'`) through Dexie outbox + `upsertMatchEnd` action + match_ends column. Owner: Phase 12. Discovered: Phase 8d follow-up, 2026-04-29.
- [ ] **Two-commit rule for schema-dependent application changes.** Migration 029 (`matches.submitted_by_team_id`) was bundled into Commit 4 (`c65ff99`) with the dependent action / component / test changes, then the bundle was committed and pushed BEFORE the migration was applied to cloud. The action/component layer landed referencing a column that didn't exist on cloud → `_data.ts` SELECT 400/42703 → scorecard route 404'd on every load. Same flavour as Findings 10/11 from earlier today. Original Commit 4 plan required stop-and-greenlight on the migration review BEFORE writing dependent code; that gate was effectively skipped — migration was written and shown ("STOP, show me"), user said "go", and the next ~600 lines of action/UI/tests landed in the same commit instead of being split. **Process fix:** schema migrations land as their own atomic commit, pushed and verified on cloud, BEFORE any application code that depends on the new schema. Two-commit minimum: (1) migration only, push + `migration list --linked` confirm + types regen; (2) application code on top. Pattern applies to Phase 9 (booking schema), Phase 10 (T20 schema), Phase 11 (Resend integration), and any subsequent migration. Owner: **Phase 13 process audit** — codify the two-commit rule in the `phase-discipline` skill so it surfaces in every future phase brief. Discovered: Phase 8d follow-up retrospective, 2026-04-28.
- [x] ~~**Migration 029 RLS integration tests deferred — Docker unavailable.** `tests/rls/matches.test.ts` ships seven new cases that exercise migration 029 (first-submission audit, DB-pin null reject, opponent-claim block, freeze on re-submit, CHECK constraint, plus updates to two pre-existing cases that now require `submitted_by_team_id`). They could not run in the Phase 8d follow-up session because Docker wasn't running in WSL → no local Supabase stack → `npm run test:integration` had nothing to attach to. The `npm test` suite (54 files / 517 tests) passed clean and the cloud REST replication confirmed the column is selectable, but the trigger-side guarantees aren't yet exercised end-to-end. **Next session with Docker up:** `supabase start && npm run test:integration` and verify all 7 new cases plus the 2 updated cases pass. If anything fails, treat as a Migration 029 regression and patch before the next phase. Owner: next browser-QA / RLS session. Discovered: Phase 8d follow-up, 2026-04-28.~~ Closed Phase 9-prep local verification, 2026-04-29. Docker up, `supabase db reset --local` applied all migrations 001-031 cleanly, `npm run test:integration` ran end-to-end against the local stack: **11 RLS test files / 70 tests green**, including all migration-028/029/030 trigger guards and cancel_own_booking RPC paths. Phase 8e-3 surfaced a latent test-isolation bug in `seedOwnBooking` (overlapping rinkA windows across tests) — fixed in commit `f15455a` (`tests/rls/bookings.test.ts` now allocates a fresh greens+rinks pair per seed call). No migration regressions surfaced.
- [ ] **State-machine-vs-surface audit.** Every state machine introduced by a migration must be checked against every consuming surface in the same phase. `submission_status` (migration 026) was added mid-Phase-8 and surfaces were never audited end-to-end — Phase 8d burned five fix rounds on the same root pattern (Finding 4, 10/11, 14, 17). Phase 13 audit task: enumerate every state column on every state-bearing table (`tournaments.status`, `matches.status`, `matches.submission_status`, `matches.finalized_by_admin`, `bookings.status`, `tournament_entries.status`, plus any others); list every surface that reads from that table; confirm each surface's query + derivation + component branches account for every state value. Build a state×surface matrix and check it in. Owner: Phase 13. Discovered: Phase 8d Finding 17, 2026-04-29.
- [ ] **Admin per-match Submissions drill-down modal.** Design `handibowls/project/page-detail.jsx:47-71` specifies a per-match Submissions drill-down modal showing captain submitted / opponent confirmed / admin verified states with timestamps + override button. Maps cleanly onto the matches schema after migration 029 (`submission_status`, `captain_submitted_at`, `opponent_confirmed_at`, `submitted_by_team_id`, `finalized_by_admin` all available). Currently unbuilt — admin verification happens via the bulk Scoring tab today. Owner: Phase 12.5 design fidelity polish, OR a dedicated admin feature commit if/when the bulk-only flow becomes a friction point. Discovered: Phase 8d Finding 17 design audit, 2026-04-29.
- [x] ~~**Phase 9 admin booking surfaces (carved out from old Phase 9, deferred from Phase 8e).** Plan renumber in 8e-prep split the original Phase 9 into Phase 8e (player) + Phase 9 (admin). Admin scope: `/manage/greens` weekly availability editor against `booking_windows` + rink-disable toggle; `/manage/overview` "Bookings" calendar tab with weekly grid + force-book / force-cancel override + audit entry. Likely needs a new `admin_force_cancel_booking(uuid, reason text)` SECURITY DEFINER RPC parallel to the player's `cancel_own_booking` (migration 030) so the admin override writes an audit row alongside the cancel — audit-table decision deferred to that phase. Owner: Phase 9. Discovered: Phase 8e-prep, 2026-04-29.~~ Closed Phase 9 close, 2026-04-29. Three sub-checkpoints: 9-prep (`6212098`) shipped migration 031 (generic `audit_log` table + `audit_log_visible_to_admin(table_name, row_id)` SECURITY DEFINER helper + `admin_force_cancel_booking(uuid, text)` RPC writing the cancel + audit row in one transaction); 9-1 (`f8c10e3`) shipped `/manage/greens` weekly availability editor + per-rink disable toggle; 9-2 (`4e7fed6`) shipped `/manage/overview` Bookings tab (`BookingsCalendarGrid` weekly view + `BookingDetailSheet` force-cancel form + `adminForceCancelBooking` action with full SQLSTATE→result-kind mapping); 9-3 (this commit) shipped `AuditLogPanel` reading from `audit_log` for the admin's club. Force-book RPC explicitly skipped — admins use existing `bookings_club_admin_rw` INSERT permission; force-cancel + re-book = two audit rows = clean trail.
- [ ] **Fair-Rink hints — soft-deprioritisation algorithm.** Plan original Phase 9 step 6 ("Booking UI soft-deprioritises rinks most-allocated in tournaments this season"). Algorithm not specified — needs a heuristic (rolling 30-day allocation count? season-to-date? weighted by match-importance?), an admin-tunable threshold, and a render contract on the player /book slot grid (muted vs emphasised rink chips). Surfaces on player UI but the admin-side configuration + algorithm itself belongs alongside the rest of the admin booking work. Owner: Phase 12.5 design fidelity polish (algorithm-definition scope is too design-research-heavy for a feature phase). Discovered: Phase 8e-prep, 2026-04-29.
- [x] ~~**`AuditTab` empty-state copy fix.** `app/(club-admin)/manage/tournaments/[id]/_components/tabs/AuditTab.tsx` empty state currently references "audit_log table lands in Phase 12". Migration 031 (Phase 9-prep) shipped that table; the copy is now a stale lie. Replace with `'No audit events recorded for this tournament yet'` (or equivalent). Split from the original AuditTab retrofit entry at Phase 12 triage, 2026-04-30. Owner: Phase 12 (12-2 sub-checkpoint). Discovered: Phase 9-3, 2026-04-29.~~ Closed: Phase 12 / 12-2, 2026-04-30. Headline now reads "No audit events recorded for this tournament yet" with a neutral explainer listing the event kinds (status changes, score edits, round advances, withdrawals). No phase reference in the rendered copy. Pinned by `tests/components/tournament/AuditTab.test.tsx` — 4 cases asserting the new headline + the absence of "Phase 12" / "audit_log table lands" strings + filter chip + disabled-affordance preservation.
- [ ] **`AuditTab` retrofit — read tournament-domain audit rows.** Once `audit_log_visible_to_admin(table_name, row_id)` extends to `tournament` rows AND tournament admin actions (status changes, score overrides, round generation) write audit rows via SECURITY DEFINER RPCs, wire `AuditTab` to read `audit_log` filtered by `table_name='tournaments' AND row_id=tournamentId` reusing the `AuditLogPanel` row layout from Phase 9-3. Multi-phase work (helper extension + RPCs + tab wire). The minimal sequence: (1) extend the visibility helper with an `elsif p_table_name = 'tournaments'` branch resolving via `tournaments.club_id`; (2) introduce SECURITY DEFINER RPCs for tournament admin actions; (3) wire `AuditTab` to the new query. Co-requires tournament admin SECURITY DEFINER RPCs that don't yet exist. Split from the original AuditTab retrofit entry at Phase 12 triage, 2026-04-30 — re-owned from Phase 12 to Phase 13. Owner: Phase 13. Discovered: Phase 9-3, 2026-04-29.
- [ ] **Audit-log fetcher booking pre-fetch cap — 500 rows.** `getRecentAuditLogForClub` (`app/(club-admin)/manage/overview/_data.ts`) explicitly filters audit rows by `row_id IN (last 500 booking IDs ordered by created_at desc)` to scope multi-club admins to the host club. The 500 cap is enough for ~12-18 months of admin actions on a typical club, but is a soft ceiling: an audit event on a booking older than the 500th most recent would not surface. RLS via `audit_log_visible_to_admin` already enforces club access; the explicit filter just narrows for display. Right fix when the cap bites: a SECURITY DEFINER `recent_audit_log_for_club(p_club_id uuid, p_limit int)` RPC that does the join in SQL and returns audit rows directly. Not Phase 13 because the cap is fine for v1 — a real bite would surface as "I cancelled an old booking and don't see it in the panel," at which point upgrade. Owner: Phase 12.5 (revisit when on-the-ground usage data exists). Discovered: Phase 9-3, 2026-04-29.
- [ ] **Audit-log read flagged generically when audit fetch fails but bookings succeed.** `/manage/overview/page.tsx` runs the bookings fetch first (which establishes the host-club resolution), then `getRecentAuditLogForClub`. If the audit query fails (DB hiccup, transient PostgREST error) the calendar still renders — the panel shows its `errored` placeholder. Today the log includes only the underlying error message via `console.error`; the admin-side toast that surfaces in the BookingDetailSheet flow doesn't fire here because the page is server-rendered. Acceptable for v1 (the user can refresh) but if audit becomes load-bearing for compliance / dispute resolution, add Sentry-style telemetry on the fetcher's error branch. Owner: Phase 12 (telemetry sweep). Discovered: Phase 9-3, 2026-04-29.
- [ ] **Server-only module poisoning risk audit.** Phase 8e-2 surfaced that `_data.ts` files marked `import "server-only"` cannot export RUNTIME values (functions, label maps, constants) that are imported as runtime values by Client Components without breaking Next's build. Type-only imports (`import type { Foo } from "../_data"`) are safe because TypeScript erases them — and that's how every existing `_data.ts` is consumed today. The risk is silent: a future commit adding `export const PURPOSE_LABELS = ...` to a `_data.ts` and importing it from a Client Component would trip the server-only boundary check at build time only. Fixed for `book/_data.ts` by splitting types + pure helpers into `slots.ts` (no `'server-only'` directive). Phase 13 audit task: sweep every `_data.ts`; for any runtime value (non-type) export, confirm no Client Component imports it as a runtime value, OR extract it into a sibling module without the directive. Lint rule candidate: ESLint custom rule `no-runtime-import-from-server-only` flagging `import { x } from "*/_data"` from `'use client'` modules where `x` isn't `import type`. Owner: Phase 13. Discovered: Phase 8e-2, 2026-04-29.
- [ ] **RLS test cleanup hardening — orphan rows from ON DELETE RESTRICT.** Phase 8e Finding 18 diagnosis surfaced 4 leftover `bookings` rows in cloud DB at 2 unique `test-...` clubs from interrupted `tests/rls/bookings.test.ts` runs. `tests/rls/helpers.ts` `cleanup()` deletes test users (cascade) + clubs, but `bookings.club_id` has `ON DELETE RESTRICT` (migration 005) — when a test crashes mid-run with bookings already inserted, the club delete silently fails (catch-less promise rejection inside `afterAll`). Same RESTRICT pattern likely exists for other relationships across the schema (matches.tournament_id is CASCADE so safe; `tournament_entries`, `t20_assessments` etc. should be audited). Orphans are invisible to player surfaces (RLS filters by club membership) so not blocking, but they're real test debt + a forensic-noise tax when reading cloud DB during diagnostics. Phase 13 audit task: extend `cleanup()` to delete bookings (and any other RESTRICT children) `WHERE club_id = ANY(clubIds)` BEFORE the clubs delete; sweep migrations for similar RESTRICT relationships in test-touched tables; one-off cleanup pass for the existing 4 orphans. Owner: Phase 13. Discovered: Phase 8e Finding 18 diagnosis, 2026-04-29.
- [x] ~~**Lighthouse PWA gates — plan reconciliation.** Lighthouse 12+ removed the PWA category. `HANDIBOWLS_REBUILD_PLAN.md:481, 715, 989, 1081` still assert "Lighthouse PWA ≥ 95" on player routes (and `≥ 90` on Phase 3) — structurally unverifiable on Lighthouse 13.1.0 (`categories.pwa: undefined`). The legacy individual audits (`installable-manifest`, `service-worker`, `themed-omnibox`, `splash-screen`, `maskable-icon`, `apple-touch-icon`) are also gone from default runs. **`scripts/lighthouse-pwa.mjs` (Phase 8f-3) no longer asserts PWA — `n/a` is structural.** Manifest + SW + icons validated by build + asset-read; runtime installability verified during human-driven mobile QA on real Android Chrome (per PHASE_LOG.md). Rewrite the plan's PWA gates to reference the new structural checks (manifest validates / SW registered / 4 PWA-required icons exist / apple-touch-icon emitted / `beforeinstallprompt` fires on a real device) rather than the dead Lighthouse score. Re-owned from Phase 8g (closed without the edit) to Phase 12 plan reconciliation at Phase 12 triage, 2026-04-30 — split from the original Lighthouse PWA entry; the Lighthouse-11 pinning sub-task closes alongside. Owner: Phase 12 (12-7 close). Discovered: Phase 8f-3, 2026-04-29.~~ Closed: Phase 12 / 12-7 audit, 2026-04-30 — already at parity. Phase 8g's "PWA gate realignment" note (HANDIBOWLS_REBUILD_PLAN.md:485-505) had already done the rewrite: every prior "Lighthouse PWA ≥ 95" assertion was superseded by structural checks (manifest validation, SW registration, real-device install on Android Chrome + iOS Safari, offline shell load). The two remaining PWA mentions in the plan (line 486, line 1020) are explanatory references to the realignment, not assertions. Drift entry was open against a stale plan state; the entry itself outlived the work. No further plan edit required.
- [x] ~~**Lighthouse-11 pinning option.** Pin `lighthouse@11` in `scripts/lighthouse-pwa.mjs` to recover a single numeric PWA score as belt-and-braces alongside the structural checks.~~ Closed: Phase 12 triage (`9f77b73`), 2026-04-30 — Lighthouse 11 is EOL (no Chrome compat updates); pinning is not a real path. Structural checks (per the Lighthouse PWA gates plan reconciliation entry above) are the v1 contract. Split from the original Lighthouse PWA entry at Phase 12 triage.
- [x] ~~**Player route Performance scores below the 90 gate — primary suspect: 1.4MB chunk.** Phase 8g production-build re-run (`next start`, source maps off, mobile preset, simulated throttling) reports Performance: `/play` 62, `/book` 77, `/tournaments` 77, `/me` 73. Materially better than the Phase-8f-3 `next dev` numbers (44 / 34 / 43 / 44) but still all below the ≥90 bar from `HANDIBOWLS_REBUILD_PLAN.md` Phase 13 success criteria. **`/play` is the worst-scoring surface** with these core metrics: FCP 1.4s ✓, LCP **4.4s** ❌, TBT **1,040ms** ❌, CLS 0 ✓, Speed Index 2.2s ✓, TTI 5.5s. Main-thread breakdown: Script Evaluation 2,092ms (the bulk), Other 1,344ms, Style & Layout 602ms. **Primary lead**: a single `.next/static/chunks/03uasr3qxc~3_.js` chunk weighs **~1.4MB** on disk — strongly suspect this is admin/PDF / heavy-deps leaking into the player main bundle (`@react-pdf` 3.1MB raw + `@tanstack` 9.5MB raw + `dexie` 3.2MB raw all candidates; `dexie` is scorecard-only and shouldn't ship to /play). Top unused JS: chunk `0q1ilu` 81% wasted (42KB unused / 52KB total). Accessibility 92-96, Best Practices 100, SEO 91 — healthy. **Triage scope: Phase 12.5 polish.** Steps: (1) `npx @next/bundle-analyzer` to confirm the 1.4MB chunk's contents; (2) lazy-load `BookingSheet`, `OpponentConfirmationCard`, `DisputeForm` via `next/dynamic({ ssr: false })` — three sheet/modal Client Components currently statically imported into the player layout chunk; (3) verify `lucide-react` tree-shaking (consider deep imports `lucide-react/dist/esm/icons/<icon>`); (4) move `@react-pdf` import boundary to ensure it never reaches player chunks; (5) audit font-loading via `next/font` for `Barlow Condensed Italic Black 900`. Reports per surface saved to `scripts/lighthouse-pwa/<slug>.json` (gitignored) for triage. Owner: Phase 12.5. Discovered: Phase 8f-3, updated Phase 8g, 2026-04-29.~~ Closed: Phase 12 / 12-5, 2026-05-01. Six-step plan executed; bundle-side wins are unambiguous, Lighthouse-side gap remains. **(1) Bundle analysis** via Turbopack-native `next experimental-analyze` (then a custom `scripts/route-bundle-audit.mjs` for headless per-route Client Component graph readout — the experimental command is browser-UI-only, no JSON). The 1.4MB chunk hypothesis is **disproven**: it's `node_modules/yoga-layout/dist/binaries/yoga-wasm-base64-esm.js` (wasm dep of @react-pdf/renderer) and is loaded by `/manage/tournaments/[id]/pdf` only — properly gated by `PdfPreview.tsx`'s existing `next/dynamic`. Either the original drift attribution was a misread, or a 12-3/12-4 commit inadvertently fixed it. **(2) Lazy-load** of BookingSheet (in /book), DisputeForm + OpponentConfirmationCard (in scorecard) shipped via `next/dynamic({ ssr: false })`. **(3) Lucide** tree-shaking already healthy — all imports across the repo are named-import form; no barrel/star imports surfaced. **(4) @react-pdf gate** was already done at 12-5 open per the audit. **(5) Dexie gate** shipped via lazy-mounting `DynamicSyncBadge` (new `DynamicSyncBadgeMount` "use client" wrapper) — dexie chunk now no longer in /play /book /tournaments /me's Client Component graph (audit confirms `dexie=no` on all four). **Bundle deltas:** /play 581→484 KiB (−17%), /book 929→491 KiB (−47%), /tournaments 583→485 KiB (−17%), /me 585→488 KiB (−17%), scorecard 947→646 KiB (−32%). **(6) Lighthouse re-run** (3 runs, WSL Lighthouse 13, mobile preset, production build): /play median Perf 65, /book 70, /tournaments 66, /me 71, scorecard 70. **All four base routes still below 90.** Single-run noise in WSL is significant (range 47–73 across runs for the same route). Real-device Lighthouse confirmation is still pending. Phase 13 follow-up entry opened below for the sub-90 gap; M3 (L42) tournament-detail Lighthouse closure entry references this.
- [ ] **[L67-followup] Player routes still under 90 Lighthouse perf in WSL after 12-5 bundle reductions.** Phase 12 / 12-5 shipped the L67 six-step plan and the bundle audit confirms unambiguous wins (chunk reductions of 17–47% across /play /book /tournaments /me; dexie+yoga properly gated). But Lighthouse 13 mobile-preset runs from WSL2 still show median Performance below 90 on every player route after fixes (/play 65 · /book 70 · /tournaments 66 · /me 71 · scorecard 70 — three-run medians). Single-run noise is large (/book ranged 47–72 across three runs) which strongly suggests WSL CPU throttling is contaminating the score. Real-device Lighthouse on Android Chrome (the canonical Phase 8 mobile QA target) is the authoritative gate. **Phase 13 task:** (a) re-run Lighthouse from a real Android device + a real laptop browser — if scores clear ≥90 in those environments, close out; (b) if real-device numbers also lag, the next leads are (i) above-the-fold image optimisation (player home hero render), (ii) confirm React 19.2 streaming hydration is firing for the player layout, (iii) `next/font` self-hosting audit for `Barlow Condensed Italic Black 900`, (iv) tighten LCP via `priority` on the home hero. **M3 / L42 followup folded into this entry:** `/manage/tournaments/[id]` desktop scored Performance 88 + Accessibility 85 in the same WSL run; the perf gap likely shares WSL noise root cause; the Accessibility 85 is its own audit (separate from this entry's perf scope) and gets its own Phase 13 sub-task: enumerate landmark/heading rhythm + button/link aria gaps on `/manage/tournaments/[id]`, fix, re-audit. Owner: Phase 13. Discovered: Phase 12 / 12-5, 2026-05-01.
- [x] ~~**`matches.submitted_by_team_id` column for passive-vs-active captain rendering.** Phase 8d-prep (migration 026) added the `submission_status` enum but not a column tracking which side actually submitted. The scorecard's `<CaptainSubmittedBranch />` currently renders the action card to both captains; the submitter taps Confirm and gets the action's "Match already submitted" precondition error. UX is functional but not optimal — submitting captain should see a passive "Awaiting opponent" banner instead. Phase 12 polish: add `submitted_by_team_id uuid references tournament_teams(id)` to `matches`, populate in submitMatch, branch the scorecard render on it. Owning phase: Phase 12. Discovered: Phase 8d-prep, 2026-04-29.~~ Closed: phase-8d-followup, 2026-04-28. Migration 029 ships the column + CHECK constraint + extended trigger guard (freeze + first-submission gate + caller-membership check). `submitMatch` derives the caller's team_id and seeds the column on first submission; trigger silently restores OLD on re-submit. Scorecard's `<CaptainSubmittedBranch />` branches on `submitted_by_team_id === callerTeamId` — passive banner for the submitter, OpponentConfirmationCard for the opponent. Diagnostic 14 (captain self-confirm) closed at the schema layer, not the UI layer.
- [x] ~~**Match-ends Dexie → server sync worker.** Phase 8c lands the
  scorecard against a Dexie outbox (`public.match_ends` server table
  exists from migration 005 but the client doesn't write to it yet).
  Need a service-worker-driven flush that takes queued Dexie rows,
  upserts them to server `match_ends` via PostgREST INSERT ... ON
  CONFLICT (match_id, end_number) DO UPDATE WHERE local_updated_at >
  server_updated_at, and reconciles via the conflict modal when the
  server is newer. Owning phase: Phase 8d. Discovered: Phase 8c
  build, 2026-04-29.~~ Closed: Phase 8d, 2026-04-29. Resolved via
  client-side `useOutboxFlush` hook (lib/scorecard/use-outbox-flush.ts)
  + the `upsertMatchEnd` Server Action (lib/scorecard/actions.ts)
  enabled by migration 027 (match_ends.updated_at + participant
  UPDATE/DELETE policies). Auto-flush on online + visibility events;
  conflicts surface to `<ConflictResolutionSheet />` with three
  resolutions (use mine / use theirs / dispute). Service worker
  (Serwist via `@serwist/turbopack`) handles cache strategies for
  the player surfaces; write-side queueing lives in Dexie + the
  client flush, not in a sw BackgroundSync queue (avoids
  double-flush since both layers would otherwise replay the same
  Server Action calls).
- [x] ~~**Captain-submitted / opponent-confirmed schema gap.** The
  scorecard's state machine has 5 distinct UI states (in_progress,
  captain_submitted, opponent_confirmed, admin_verified, walkover/
  cancelled) but the `match_status` enum only ships scheduled /
  in_progress / completed / walkover / cancelled. Phase 8c renders
  captain_submitted and opponent_confirmed as the same UI ("Awaiting
  verification") gated on `status='completed' AND
  finalized_by_admin=false` — the player whose POV is the captain
  side and the opposing side both see the OpponentConfirmationCard.
  Need a migration adding two enum values + an action that
  transitions captain_submitted → opponent_confirmed → completed
  rather than the current submitMatch (sets in_progress) →
  confirmMatch (sets completed) collapse. Owning phase: Phase 8d.
  Discovered: Phase 8c build, 2026-04-29.~~ Closed: Phase 8d-prep
  migration 026, 2026-04-29. Resolved via Option (b) — separate
  `submission_status` enum (`pending` / `captain_submitted` /
  `opponent_confirmed`) + `captain_submitted_at` +
  `opponent_confirmed_at` audit timestamps + CHECK constraint pinning
  consistency. Actions updated: submitMatch sets captain_submitted +
  audit timestamp; confirmMatch transitions to opponent_confirmed
  (no longer collapses to status=completed); verifyMatch requires
  opponent_confirmed by default with override-score escape hatch for
  dispute resolution. Open follow-up captured in this same section:
  the "submitted_by_team_id" column for proper passive-vs-active card
  branching is a Phase-12 polish item (currently both captains see
  the action card; the action precondition rejects no-op confirms).

### T20 compass capture (Phase 10 output, added when shipped)

*(empty)*

### Player Twenty 20 hub (Phase 12-1 output, added when shipped)

- [x] ~~**Player `/t20` hero "Book gold assessment" CTA — scheduling backend deferred.** Design source `player-pages.jsx:182` ships a primary CTA that books the next-tier-up assessment. v1 has no Twenty 20 scheduling schema (the existing `bookings` table is rink reservations, not assessment slots), so the CTA on `/t20` routes to `/me` with an explanatory caption instead. Pre-condition for wiring it: the Scheduled-send infrastructure (deferred) entry in the Phase 12 section needs to ship — same dependency as the messaging Send-later half. Owner: Phase 12.5 (re-wire the hero CTA when scheduling lands; until then, the `/me` route is the honest v1 fallback). Discovered: Phase 12-1, 2026-04-30.~~ Closed: Phase 12-1 followup, 2026-04-30. Replaced with the request → schedule loop. /t20 now ships a single tier-agnostic "Request assessment" CTA wired to `request_t20_assessment` (migration 037) which fans out to club admins as an in-app message + notification. Admins see a "Schedule from this request" button on /manage/messages that deep-links to /manage/bookings/new with the player pre-filled; saving the form fires `admin_schedule_t20_assessment` (migration 037) which inserts a `purpose='t20_assessment'` booking + player notification atomically. Booking surfaces under "Upcoming assessments" on /t20.
- [ ] **Player `/t20` ladder Platinum tier is aspirational.** Design source `player-pages.jsx:185` shows a 4-step ladder Bronze → Silver → Gold → Platinum, but the `t20_grade` enum (migration 001) caps at `gold`. Platinum renders as a `future` step indefinitely — no row in the schema can reach it. v1 ships the 4-step ladder per design fidelity; players above gold will not see Platinum become active until BSA's grading framework formalises it. Right fix: extend the `t20_grade` enum + grading bands when BSA confirms the Platinum threshold, then update `computeLadder` in `app/(player)/(gated)/t20/page.tsx`. Owner: Phase 12.5 (waits on BSA stakeholder confirmation per the [BLOCKED] BSA seeding entry's pattern). Discovered: Phase 12-1, 2026-04-30.
- [x] ~~**Player T20 results detail view — design needed.** Players currently see hero grade pill + past-assessments list on `/t20` (the past-assessments list is additive over the design source, flagged inline at `app/(player)/(gated)/t20/page.tsx:173`). Tapping a past-assessments row currently goes nowhere — there is no player-side detail view for an individual assessment. Design source's `t20-page-results.jsx` is the admin view (`/manage/t20/[id]`); no player-facing equivalent exists in the bundle. Owner: Phase 12.5 → 12.5-4 (audit id `player-t20-results-detail`). Audit-spec: hero (grade reveal, percentage, assessor, date) + section breakdown (read-only, R1/R2/Total/% columns) + categorised notes tiles (read-only) + zone heatmap. **Locked decisions:** hand-balance + length-distribution charts DROPPED (coach analysis tools, not player motivation tools); CTA copy `"Request re-assessment"` (mirrors 12-1 `"Request assessment"`). Discovered: Phase 12 12-4 close, 2026-04-30; design specced Phase 12.5 audit, 2026-05-01.~~ Closed: Phase 12.5 / 12.5-4 (`bb1f902` + `b3cce3e` + `cff814a`), 2026-05-01. New `app/(player)/(gated)/t20/[assessmentId]/page.tsx` Server Component reads via the shared `getAssessmentDetail` fetcher (extracted to `lib/t20/assessment-detail.ts` in commit 1) — both admin + player routes consume the same data path; RLS via `t20_assessments_subject_read` (migration 010) gates row visibility; defence-in-depth `assessment.player_id !== ctx.userId` 404 guard at the route boundary; non-submitted assessments redirect back to /t20. New `<PlayerResultsView>` (320 lines) ships hero (grade gradient + percentage + assessor + date), 5-column section breakdown table (Section / R1 / R2 / Total/Max / %) with mobile <600px collapse to 2-column (Section + Total/% per audit spec), 3 read-only categorised notes tiles (Strengths / Watch / Coach focus, with empty-state per category + legacy-notes block when present), and a lazy-loaded zone heatmap. **Hand-balance + length-distribution charts NOT rendered** per locked decision. New `<RequestReassessmentButton>` Client island wraps the existing `requestT20Assessment` action (no new Server Action); idle/pending labels read "Request re-assessment" / "Requesting…"; toast copy reads "Re-assessment requested". Past-assessments rows on `/t20` now `<Link>`-wrapped with ChevronRight affordance (pre-12.5-4 they were dead taps). Heatmap lazy-load needed `next/dynamic({ ssr: false })` which Next 16 / Turbopack only allows in Client Components — landed via a tiny `<HeatmapMount>` "use client" wrapper (same pattern as 12-5's `DynamicSyncBadgeMount`). `rowsToDeliveries` + `computeZoneCounts` extracted to lib/t20/assessment-detail.ts so admin + player routes share the helpers. Pinned by 12 cases in `tests/app/player/t20-detail-view.test.tsx`; RLS contract holds via existing `tests/rls/t20.test.ts:27-48` (player-can-read-own / cannot-read-others).

### Phase 12.5 audit — new findings (Claude Design pass, 2026-05-01)

Nine new design / system findings opened by the Phase 12.5 audit at 12.5-prep. Each has a per-entry brief in [`docs/audit/phase-12.5/audit-data.js`](./docs/audit/phase-12.5/audit-data.js); the per-entry IDs below are the canonical reference. Sub-checkpoint mapping in [`DRIFT_TRIAGE_PHASE12-5.md`](./DRIFT_TRIAGE_PHASE12-5.md).

- [x] ~~**Player tab-bar diverges from shadcn Tabs.** Audit id `tabs-fork`. Player `/me/inbox` uses `tab-bar` + `tab-btn` from `player-styles.css`; admin uses shadcn `<Tabs>` (`components/ui/tabs.tsx`). Same intent, two implementations. Right fix: `components/layout/MobileTabBar.tsx` re-exporting shadcn Tabs with a `mobile-bar` skin (60px tall, 12px label, primary underline) so both surfaces consume the same primitive + ARIA semantics. Migrate `PageInbox` then delete the legacy CSS rules. Owner: Phase 12.5 → 12.5-1. Discovered: Phase 12.5 audit, 2026-05-01.~~ Closed: Phase 12.5 / 12.5-1 (`44bdee4`), 2026-05-01. New `components/layout/MobileTabBar.tsx` (87 lines) wraps shadcn `<Tabs>` (Radix) with the audit's mobile visual: 60px tall list, 12px uppercase label, primary-500 underline override of shadcn `variant="line"`'s upstream `bg-foreground`. URL-driven via `useRouter().replace(...)`; first item's value treated as the default (search-param key removed). `app/(player)/(gated)/me/inbox/_components/InboxTabs.tsx` rewritten to consume it (90 → 28 lines; same export shape so the parent page doesn't change). `.tab-bar` / `.tab-btn` from the design source's `player-styles.css` were never shipped to this repo's `app/globals.css` — no legacy rules to delete. Pinned by 8 cases in `tests/components/layout/MobileTabBar.test.tsx` (ARIA + active state + count badge + URL push semantics including default-value param-deletion + paramKey override).
- [x] ~~**Missing shared `<EmptyState>` primitive.** Audit id `empty-state-primitive`. No primitive exists for empty surfaces; rubrics empty / t20 list empty / messages Sent empty / future tournaments empty / members empty all reinvent. Right fix: ship `components/layout/EmptyState.tsx` with `{ icon?, eyebrow?, title, body?, primaryCta?, secondaryCta?, variant? }` API; centred column, mono eyebrow / display headline / body copy / primary + secondary CTAs. Foundation for 12.5-3 (`t20-list-empty-states`) and 12.5-2 (`stub-page-phase-tag` long-term rewrite). Owner: Phase 12.5 → 12.5-1. Discovered: Phase 12.5 audit, 2026-05-01.~~ Closed: Phase 12.5 / 12.5-1 (`b071f74`), 2026-05-01. `components/layout/EmptyState.tsx` (119 lines) shipped per audit spec — centred column, optional 56px lucide icon (stroke 1.75 ink-muted), eyebrow utility, display h3 (24px font-display 900 italic), body copy (15px ink-muted max 320px), primary + optional secondary CTAs (Link via `Button asChild` when `href`; plain button when `onClick`), `variant="bone"` (default, dashed-border card chrome) / `variant="on-surface"` (drop chrome for nested contexts). No consumer migration in this commit per the foundation guardrail; consumers migrate in 12.5-2 (StubPage rewrite) + 12.5-3 (t20 list empty + messages Sent empty + rubrics empty) onward. Pinned by 10 cases in `tests/components/layout/EmptyState.test.tsx`.
- [ ] **Radius scale sweep — 87 callers of `rounded-xl` (12px).** Phase 12.5-1 codified the locked radius scale (10/14/20/24, dropping 12) at the foundation level (`app/globals.css` header comment in commit `c828394`). 87 files in `app/` + `components/` still use `rounded-xl` (the dropped 12px value); shadcn primitives (`Button`, `Input`) use `rounded-lg` (8px) — also outside the scale. Sweeping all 87 is consumer-migration work, not foundation. Right fix: as each surface is touched in 12.5-2 onward, migrate `rounded-xl` → `rounded-[14px]` (or other scale value as appropriate); a final cleanup pass at 12.5-7 catches anything not yet migrated. shadcn primitives stay on their upstream rounded-lg until the v1 polish phase that revisits the primitive set (post-v1, with a Tailwind v4 token-system migration). Owner: Phase 12.5 → 12.5-7 (final pass) + post-v1 (shadcn primitive radius). Discovered: Phase 12.5 / 12.5-1, 2026-05-01.
- [x] ~~**Grade colours hex-coded inline at the admin t20 hero.** Audit id `grade-color-extraction`. `t20-page-results.jsx` heroBg gradient uses inline `#f5cf52`, `#d4a000`, `#8a6300` (gold), `#c08758` (bronze), `#2a2a28` (fail) etc; ShowcaseT20 grade-D row uses `bg-[#F5B700]` (same as grade-C). Right fix: `components/brand/grade.ts` exporting `GRADE_COLORS` keyed by `gold | silver | bronze | retry | ungraded`; both `<GradePill>` and any hero gradient consume it. **Locked decision (12.5-prep):** silver gets a FIXED gradient like gold/bronze, NOT derived from theme `--primary-*` tokens — silver should look the same across every preset. Owner: Phase 12.5 → 12.5-2. Discovered: Phase 12.5 audit, 2026-05-01.~~ Closed: Phase 12.5 / 12.5-2 (`da426b5`), 2026-05-01. New `lib/brand/grade.ts` (87 lines) exports `GRADE_COLORS` keyed by `Grade | "ungraded"` with the audit's `{ from, mid, to, ink }` shape, plus three helper exports — `gradeHeroGradient` (135deg 3-stop), `gradePillGradient` (140deg 3-stop), `gradePillCompactGradient` (120deg 2-stop legacy). Hex values for gold / bronze / fail copied verbatim from the design source's `t20-page-results.jsx` heroBg. **Silver migrated to a fixed cool-metallic gradient** `#e6e7e9 / #b1b2b4 / #6f7173` per the locked decision — pre-12.5-2 it derived from `--primary-*` and tinted with the active club preset. Three consumers migrated: `<GradePill>` (lg + sm/md variants), admin `<AssessmentResults>` hero (kept the per-tier coaching copy / band / bowlColor metadata; only the gradient + ink moved to helpers), `<ShowcaseT20>` (the C-grade hex `#F5B700` was duplicated inline at the legend row + wedge fill — de-duplicated via `GRADE_FILL` consumed by both, those zone-grades A/B/C/D stay distinct from the assessment-tier `lib/brand/grade.ts` set). Pinned by 11 cases in `tests/lib/brand/grade.test.ts`.
- [x] ~~**SpeckleField intensity steps undocumented.** Audit id `speckle-intensity-step`. Three intensities in use across consumers (player /t20 hero `1.4 / 1.3`, landing feature card `1.2 / 1.0`, auth aside default `1.0 / 1.0`) with no named scale. Right fix: `intensity?: "subtle" | "medium" | "bold"` prop on `SpeckleField` mapping to `(density, opacityScale)` pairs `1.0/1.0 · 1.2/1.2 · 1.3/1.4`; consumers pick a name, not numbers. Owner: Phase 12.5 → 12.5-2. Discovered: Phase 12.5 audit, 2026-05-01.~~ Closed: Phase 12.5 / 12.5-2 (`6ad2b17`), 2026-05-01. New `intensity?: "subtle" | "medium" | "bold"` prop on `SpeckleField` mapping to `(density, opacityScale)` pairs codified in `INTENSITY_MAP` (1.0/1.0 · 1.2/1.2 · 1.3/1.4). Explicit numeric `density` / `opacityScale` props take precedence with a dev `console.warn`. 4 of 7 production consumers + 1 of 2 dev consumers migrated to named intensities (FeatureGrid → medium; tournaments/[id] hero → bold; t20 hub hero → bold; DesignShowcase preview → medium). 3 consumers kept explicit numeric values (SocialProof 1.4/0.35, HeroNextMatch 1.4/1.6, TournamentCard 0.9/1.1, me-page 1.1/1.2) — values are outside the 3-step scale and snapping to the nearest intensity would have changed the visual contract; those carry a soft drift to revisit at 12.5-7 final pass.
- [x] ~~**`<StubPage>` body needs `<EmptyState>` rewrite.** Audit id `stub-page-phase-tag`. Phase 12 / 12-7 already dropped the leaking `phase` prop and the rendered "Coming in {phase}" tag (commit `51db553`). Long-term, `<StubPage>` should consume the new `<EmptyState>` primitive from 12.5-1 instead of duplicating the centred-text-on-card pattern, and every route currently using it (`/play` super-admin, `/payments`, `/me/inbox` placeholders) should either become a real implementation or render a real `<EmptyState>`. Owner: Phase 12.5 → 12.5-2 (depends on 12.5-1 EmptyState ship). Discovered: Phase 12.5 audit, 2026-05-01.~~ Closed: Phase 12.5 / 12.5-2 (`d5f1162`), 2026-05-01. `components/layout/StubPage.tsx` body rewritten to consume `<EmptyState>` (Construction icon + "Coming soon" eyebrow + display-h3 title + body copy explaining the route exists for redirect targeting). Component API unchanged (`{ title, eyebrow?, description? }`). The two consumers (`/payments` + `/platform/tournaments` super-admin stubs) need no updates. Long-term: per-route real implementations or surface-specific EmptyStates as each route is touched. Pinned by 3 cases in `tests/components/layout/StubPage.test.tsx` including a "no Phase N tracking-string leak" regression-guard.
- [ ] **`loading.tsx` files render unstyled spinners, not Skeleton trees.** Audit id `loading-spinner-only`. Each route's `loading.tsx` should render a Skeleton tree shaped like that route (list pages → row repeats; detail pages → hero + section blocks; admin t20 results → hero + table + 3 chart blocks). Spinner-only loaders cause layout shift on hydration. Right fix: audit `app/**/loading.tsx`, replace each body with a Skeleton tree mirroring its sibling `page.tsx`; if a route has no meaningful structure to mimic, drop the `loading.tsx`. Owner: Phase 12.5 → 12.5-6. Discovered: Phase 12.5 audit, 2026-05-01.
- [ ] **Icon stroke + size discipline drift.** Audit id `icon-stroke-scale`. Stroke widths in use: lucide default 2 (most surfaces), bespoke `IconArrow` 2.2 (landing), admin sidebar active 2.5. Sizes float (13/14/15/16/18 across player surfaces). Right fix: stroke 2 default + 2.5 only on `aria-current` / active nav item; 4-step icon-size scale `14 / 16 / 20 / 24` (inline-with-body / inline-with-heading / button-or-nav / hero-or-metric); ESLint or grep guard flagging size literals outside the scale. **Locked decision (12.5-prep):** KEEP custom `IconArrow` / `IconTournament` / `IconScore` / `IconCompass` in `landing.jsx` as a brand differentiator — formally exempt them in the system spec. Lucide everywhere else. Owner: Phase 12.5 → 12.5-6. Discovered: Phase 12.5 audit, 2026-05-01.
- [ ] **Admin t20 results charts collapse painfully <900px.** Audit id `responsive-admin-t20-charts`. `t20-page-results.jsx` charts grid uses `gridTemplateColumns:"1.2fr 1fr 1fr"` with no mobile fallback — heatmap + hand-balance + length-distribution overlap on narrow viewports. Right fix: replace inline grid with `grid grid-cols-1 gap-[18px] md:grid-cols-[1.2fr_1fr_1fr]`; charts stack single-column full-width on mobile. Consider hiding the 256-deliveries `muted text-xs` caption <480px to save vertical space. Owner: Phase 12.5 → 12.5-6. Discovered: Phase 12.5 audit, 2026-05-01.
- [ ] **Player content scroll containers don't reserve bottom-nav space.** Audit id `player-bottom-padding`. Long lists clip behind the 76px bottom-nav on mobile (e.g. `/me/inbox` notif-list). Right fix: `pb-[calc(env(safe-area-inset-bottom)+80px)]` on the `(player)/layout.tsx` main scroll container; remove ad-hoc `padding-bottom` declarations from `player-styles.css` `.mcontent`. Owner: Phase 12.5 → 12.5-6. Discovered: Phase 12.5 audit, 2026-05-01.

### Phase 12.5 pre-stakeholder QA (12.5-7 prep)

- [ ] **Comprehensive seed data needed for full-version QA.** Phase 12.5 sub-checkpoint QA walkthroughs hit gaps where the seed data doesn't cover all states a surface can render. Examples surfaced so far: T20 assessment hero states (bronze / silver / gold / fail / ungraded / history-present), failed-invite rows for resend button QA, paginated lists for search-pagination test, adjacent same-preset cards for SpeckleField `seedKey` verification, distinct grade colours for `grade.ts` extraction QA, and club_admin with zero T20 captures for the t20 list empty state walkthrough at 12.5-3 close. Before Phase 12.5 close (12.5-7 pre-stakeholder QA walkthrough), audit the seed script to ensure every shipped surface has fixture coverage for every visible state. Owner: Phase 12.5 → 12.5-7 (folds into pre-stakeholder QA preparation). Discovered: Phase 12.5 / 12.5-2 close, surfaced again at 12.5-3 close, 2026-05-01.

### Tournament admin (Phase 12-2 output, added when shipped)

- [ ] **Tournament edit page missing.** No `/manage/tournaments/[id]/edit` route exists; `updateTournament` server action doesn't exist. Tournaments can be created (`/manage/tournaments/new`) and viewed (`/manage/tournaments/[id]` with tabs for entries / draw / scoring / rinks / comms / audit) but mutations are limited to the dedicated lifecycle actions (`closeEntries`, `cancelTournament`, `seedEntries`, `generateBracket`, etc.) — there's no general-purpose "edit tournament fields" surface. 12-2 wiring of `green_ids` + `fair_rink` therefore covers create only; edits to those fields ship when the edit page does. Right fix: copy `app/(club-admin)/manage/tournaments/new/page.tsx` structure to `[id]/edit/page.tsx`, add `updateTournament` action mirroring `createTournament`, handle the tournament_greens add/remove diff on save (insert new green_ids, delete removed, no-op the unchanged set). Owner: Phase 12.5 → 12.5-5 (audit id `tournament-edit-page`). Audit-spec: 5-step wizard pre-filled from existing tournament; format-step locked once any match has a score; `updateTournament` action with optimistic-locking on `updated_at`. **Locked decisions:** land on Step 1 by default (don't remember last visited step); allow renaming after publish with soft-warn that public links update. Discovered: Phase 12-2, 2026-04-30; design specced Phase 12.5 audit, 2026-05-01.

---

## Other phases (non-design drift)

### Phase 5 — Profile setup

- [x] ~~**Signup first_name / last_name fields unused.** Rendered + submitted but `signUpAction` only uses email + password. Wire in Phase 5. Files: `app/(auth)/signup/`, `lib/auth/actions.ts signUpAction`. Discovered: Phase 3 follow-up.~~ Closed: 5b, 2026-04-28. Wired through both signUpAction (form → service-client profile update) and acceptInviteAction (invite row → service-client profile update). lookupInvite returns first/last for /me/setup prefill.
- [x] ~~**Signup "check your inbox" success card unreachable.** Component built but `signUpAction` redirects to `/me/setup`. Decide at Phase 5 whether to wire or delete. Discovered: Phase 3 follow-up.~~ Closed: 5b, 2026-04-28. Resolved by deletion. Audit found no separate component file — the "dead success card" was a comment block in signup-form.tsx describing the unreachable state. Block removed; no email-confirmation flow planned.
- [x] ~~**Invite page copy says "seven days", schema is 14.** `app/(auth)/invite/[token]/page.tsx:70` hardcodes "Invites last seven days" but `invites.expires_at` defaults to `now() + 14 days` (migration 011). Fix to "14 days" or read from the invite row. Owning phase: 5b. Discovered: Phase 5a, 2026-04-28.~~ Closed: 5b, 2026-04-28.

### Phase 6 — Tournament engine

- [x] ~~**lib/tournaments primitives use uppercase enum vocab.** `labels.ts` and `handicap.ts` reference `SINGLES`/`SCRATCH`/`HANDICAP_START` but Phase 2 schema enums are lowercase (`singles`/`scratch`/`handicap_start`). Tests pass in isolation; modules can't be wired to current DB rows without case-mapping. 6b adapters must address — either case-map at adapter boundary or normalise primitives to lowercase. Recommend case-map at adapter (contained, doesn't rewrite tested logic). Owning phase: 6b. Discovered: Phase 6a, 2026-04-29.~~ Closed: 6b (`7caa5c4`), 2026-04-29. `lib/tournaments/adapters.ts` ships `dbStatusToPrimitive` / `dbFormatToPrimitive` / `dbHandicapRuleToPrimitive` (lowercase → uppercase) plus the reverse `primitiveStatusToDb` for inserts. Both directions covered by tests with explicit round-trip assertions. Primitives stay uppercase as recommended.

### Phase 7 — Admin tournament UI

- [x] ~~**`closeEntries` gate semantics — Phase 7 mirror verification.** `closeEntries` sets `tournaments.entries_close_at = now()` rather than flipping a status field, because the schema has no "entries-closed-but-not-started" state. The Phase 7 admin UI must gate "accepting entries" on `(status='open' AND (entries_close_at IS NULL OR entries_close_at > now()))`. Status only moves to `'in_progress'` when `generateBracket` creates round 1. **Verification step:** read `app/(club-admin)/manage/tournaments/[id]/` and `app/(club-admin)/manage/tournaments/_data.ts` to confirm the gate is wired correctly; if confirmed → close as already-shipped; if not → inline fix in 12-2. Re-owned from Phase 7 (closed) at Phase 12 triage, 2026-04-30. Owner: Phase 12 (12-2). Discovered: Phase 6d, 2026-04-29.~~ Closed: Phase 12 / 12-2, 2026-04-30 — verified shipped at Phase 7. Canonical gate lives at `components/tournament/EntriesGatePill.tsx:24-37` `deriveEntriesGate`. The pure helper returns `'open' | 'closed' | 'draft' | 'in_progress' | 'completed' | 'cancelled'` keyed off `(status, entries_close_at)` exactly per spec: status='open' AND (entries_close_at IS NULL OR entries_close_at > now()) → 'open'; status='open' AND entries_close_at <= now() → 'closed'; non-'open' statuses pass through. Both the list page (`StatusPill` via `deriveDisplayState`) and the detail page (`TournamentHero`) call it; both list and detail go through this primitive per the file's own header doc ("All callers that surface this concept ... MUST go through this primitive"). Already pinned by `tests/components/tournament/EntriesGatePill.test.tsx`. No code changes required at 12-2 — the verification is the closure.
- [x] ~~**Tournament batch RPCs missing from rebuild migrations.** Pre-rebuild had `admin_finalize_matches_batch`, `bulk_save_match_scores_batch`, `save_round_fixtures_batch` (308 lines, three PL/pgSQL functions in deleted `supabase/migrations/20260422_tournament_batch_rpcs.sql`). Needed by Phase 7 admin UI for atomic batch fixture editing and bulk scoring; the singular routes also relied on them for winner-propagation + OPEN→SCHEDULED transitions. Restore or rebuild in Phase 7. Owning phase: Phase 7. Discovered: Phase 6a, 2026-04-29.~~ Closed Phase 8g triage, 2026-04-29. All three RPCs landed during Phase 7: migration 023 (`save_round_fixtures_batch`), 024 (`bulk_save_match_scores_batch`), 025 (`admin_finalize_matches_batch`). Admin UI consumes them via `bulkSaveMatchScores` + `finalizeMatchesBatch` server actions.

### Phase 10 — Twenty 20 assessment module

- [ ] **`addSecondMarker` writes composite "Name · ACCRED" into a single column.** `t20_assessments.second_marker_name` is a single text column today (migration 007); the brief / design source captures both the marker name and an accreditation ID at addSecondMarker time. The Phase 10-2 action concatenates them as `"<name> · <accred>"` so both values persist, but consumers can't query / index / display them independently. Right fix: split into `second_marker_name` + `second_marker_accreditation_id` columns in a future migration, backfill the existing rows by splitting on " · " (one-shot), update `addSecondMarker` + the results-view rendering. Owner: Phase 12 (alongside any other v2 schema splits) or earlier if the second-marker UX needs the split sooner (e.g. for an accreditation-validity check at activation). Discovered: Phase 10-2, 2026-04-29.
- [ ] **`activateRubricVersion` is sequential UPDATE+UPDATE, not an RPC.** The activate flow flips `is_active=false` on the current active row, then `is_active=true` on the target — two separate UPDATE statements rather than one atomic SECURITY DEFINER function. Race window is sub-millisecond from a single request (both writes happen back-to-back from the action), but the partial unique index `t20_rubric_versions_one_active` means a concurrent activator could trip a unique violation. v1 risk is low — super-admin only, infrequent operation, no concurrent admin path documented today. Right fix when it bites: promote to a `activate_rubric_version(p_rubric_id uuid)` RPC that wraps both UPDATEs in a single transaction. Pattern is established (Phase 9's `cancel_own_booking` migration 030 + `admin_force_cancel_booking` migration 031 both follow this shape). Owner: Phase 13, OR earlier if a concurrent-activation incident surfaces. Re-owned from Phase 12.5 at Phase 12 triage, 2026-04-30. Discovered: Phase 10-2, 2026-04-29.
- [x] ~~**List page: coach-no-captures empty state not implemented.** The design source's `t20-page-list.jsx` ships three empty states; 10-4 ships two (no-data + no-match). The third — "You haven't completed any assessments yet" with a coach-self-portrait avatar circle and "Start one now" CTA — fires only when an assessor filters the list to their own captures. v1 has no assessor filter today, so the state is unreachable. When coach-scoping lands (likely Phase 12 polish or whenever the list grows enough to need per-assessor views), wire the third empty state. Owner: Phase 12.5 → 12.5-3 (audit id `t20-list-empty-states`, sub-bullet a). **Locked decision (12.5-prep):** total-only empty state at v1, not per-rubric. Discovered: Phase 10-4, 2026-04-29.~~ Closed: Phase 12.5 / 12.5-3 (`81c8aa1`), 2026-05-01. The hand-rolled `EmptyNoData` (big-bowl hero) + `EmptyNoMatch` (Search-icon centred card) now consume the shared `<EmptyState>` primitive shipped at 12.5-1. Per the locked decision, total-only empty-state copy at v1 (audit verbatim): icon ClipboardList, eyebrow "NO ASSESSMENTS YET", title "Capture your first Twenty 20", body "Pick a player, run them through the 7 sections, sign off.", primary CTA → `/manage/t20/new`. The audit's third sub-state (coach-no-captures filtered to own captures) doesn't apply at v1 since there's no assessor filter today; revisit when coach-scoping lands.
- [x] ~~**List page: filter state is React-local, not URL-driven.** `AssessmentsListClient` keeps search / status / grade in `useState`. Refresh or share-the-URL drops the filter selection. For a list that's likely to be filtered to the same view repeatedly (e.g. "completed silver assessments this quarter") shareable URLs become useful. Right fix: lift state to `?status=&grade=&q=` search params + Server Component re-fetch (or read params client-side and apply before useState seed). Not blocking — single-coach workflows fit local state fine. Owner: Phase 12.5 → 12.5-3 (audit id `t20-list-empty-states`, sub-bullet b). Discovered: Phase 10-4, 2026-04-29.~~ Closed: Phase 12.5 / 12.5-3 (`81c8aa1`), 2026-05-01. Status + grade chips push immediately to `?status=…&grade=…`; search input pushes `?q=…` after a 300ms debounce. `clearFilters()` drops all params. Pattern matches `/platform/clubs` from 12-7's search-pagination fix (mock pattern lifted from `ClubsSearch.test.tsx`). Test rewrite landed in the same commit (21 → 25 cases): URL-state seeded in `mockSearch = new URLSearchParams("...")` for filtered-render assertions; click tests verify `replaceSpy` was called with the right href; new debounce-timing case pins the 300ms contract.
- [x] ~~**List page: remove unwired Export CSV button.** Visible in the hero action bar per design source but the `onClick` is currently a no-op — no CSV export endpoint exists for Twenty 20 assessments. **User call at Phase 12 triage, 2026-04-30:** remove the button (don't build the endpoint). The CSV export can come back in a post-v1 phase if coach demand surfaces; today the disabled-CTA pattern is a v1 lie. Owner: Phase 12 (12-4 sub-checkpoint). Discovered: Phase 10-4, 2026-04-29; reworded Phase 12 triage, 2026-04-30.~~ Closed: Phase 12 / 12-4, 2026-04-30. Button removed from `app/(club-admin)/manage/t20/page.tsx`; `Download` lucide import dropped alongside (was only used for the button's icon). Hero action bar now ships the New Assessment CTA only.
- [x] ~~**New form: remove disabled Save-as-draft button.** The "Save as draft" CTA in the T20 New form's footer is rendered but `disabled` with a tooltip "Draft persistence ships with the capture wizard". The schema's `t20_assessments.status='draft'` state is the same row the capture wizard hydrates from; the simpler v1 flow is "fill the New form → Start capture creates the row → first delivery establishes the draft." **User call at Phase 12 triage, 2026-04-30:** remove the disabled button (don't wire `saveAssessmentDraft`). The Start-capture path covers the same intent; the disabled-CTA pattern is a v1 lie. Owner: Phase 12 (12-4 sub-checkpoint). Discovered: Phase 10-5, 2026-04-29; reworded Phase 12 triage, 2026-04-30.~~ Closed: Phase 12 / 12-4, 2026-04-30. Disabled `data-slot="draft-cta"` button removed from `NewAssessmentForm`. The Start-capture path covers the same intent (createAssessment writes status='draft'; first delivery establishes the row).
- [ ] **Capture wizard: distance-bucket capture not surfaced.** Migration 032 added the `t20_distance_bucket text` column on `t20_deliveries` (CHECK-constrained to `('<10cm','10-30cm','30cm+')` or NULL) per plan §13 step 0, but the capture wizard doesn't render a bucket selector. v1 records pass `distance_bucket=null` to `recordDelivery` (the action's Zod schema is null-safe). Per plan §13 step 6, distance buckets surface when the v2 rubric flips `distanceBucket: { required: true }` — until that rubric is uploaded + activated, the column stays nullable and unused. Right fix: extend `CompassPicker` (or render a secondary bottom-sheet picker) keyed off the active rubric's `distanceBucket` config. Owner: Phase 12.5 (waits on v2 rubric authoring per plan §13 step 6). Re-owned from Phase 10 follow-up (closed) at Phase 12 triage, 2026-04-30. Discovered: Phase 10-6, 2026-04-29; pre-flagged in migration 032's design notes.
- [x] ~~**Capture wizard: no Cancel-with-confirmation dialog.** "Save & pause" sends straight to `/manage/t20` with no confirmation. The design source treats every delivery as already saved (per-tap autosave via `recordDelivery`), so there's no "discard changes" path — but field testing may surface coaches wanting an "I tapped the wrong cell, undo" gesture. Today the only way to overwrite a wrong tap is to re-tap the same cell with a different option (`recordDelivery` UPSERTs). Owner: Phase 12.5 → 12.5-3 (audit id `t20-cancel-confirm`). Audit-spec: shadcn `<AlertDialog>` titled "Discard this assessment?"; body shows current grade-projection ("Projected: SILVER · 64%"); fires only when `shotsRecorded > 0`. Discovered: Phase 10-6, 2026-04-29.~~ Closed: Phase 12.5 / 12.5-3 (`5b5b36d` + `7f4792c`), 2026-05-01. Two-commit shape: `5b5b36d` ships the shadcn AlertDialog primitive (`components/ui/alert-dialog.tsx`, 192 lines, mirrors the existing `dialog.tsx` structure). `7f4792c` adds the `discardAssessment` Server Action (DELETE assessment + cascade deliveries via FK; status guard so only drafts can be discarded; silent-RLS-denial guard mirroring the 12-4 finalize hotfix pattern) + the wire-up in `CaptureWizard.tsx`. The X close button (aria-label rewritten "Save and exit" → "Discard this assessment") branches: 0 shots → silent route to /manage/t20; ≥1 shot → AlertDialog opens with audit-verbatim copy ("Discard this assessment?" / "You've recorded N shots across M sections. Discarding can't be undone."), a projection pill rendering the live grade + percent computed via a new `computeProjection(rubric, deliveriesMap)` helper (sums per-section-round subtotals; same math `aggregateAssessment` would do without a Delivery[] rebuild), and "Discard assessment" / "Keep editing" actions. "Save & pause" stays as-is for explicit save-and-resume. Pinned by 4 new cases in `tests/app/club-admin/t20-capture-wizard.test.tsx` (zero-shots silent-route + dialog open with shot count + Keep-editing close + Discard-assessment routes after action call).
- [ ] **PDF template + changelog export.** Two surfaces share the same blocker (the PDF renderer doesn't exist yet). Consolidated from two separate entries at Phase 12 triage, 2026-04-30:
  - **Results view PDF template.** `requestPdfExport` returns `kind='pending'` until the template ships. UI surfaces `toast.message("PDF generation pending")`. Hook is wired and ready — the action body just needs to swap `return { kind: "pending", reason: "template_not_ready" }` for the real renderer call (e.g. `@react-pdf` or a server endpoint streaming the PDF). When the template ships: persist the resulting URL to `t20_assessments.pdf_url` so future "Download PDF" clicks short-circuit to the cached file. Placeholder pattern documented in commit `930c9fd`.
  - **Rubrics page Export changelog button.** Visible in the versions table header per design source but disabled with tooltip "Changelog export ships with the PDF template follow-up". Conceptually the changelog is a CSV/PDF export of the diff history across all activated versions. Pairs with the PDF template work — or ships as a JSON endpoint earlier if super-admin tooling needs it sooner.
  Owner: separate Claude Design follow-up — visual polish is out of scope for the rebuild plan's defined phases. Discovered: Phase 10-7 + Phase 10-8, 2026-04-29.
- [x] ~~**Results view: R1/R2 round splits are even-half presentation, not engine-derived.** `aggregateAssessment` returns combined section totals but doesn't carry per-round breakdown. The results-view section-breakdown table renders R1 and R2 as `Math.round(total/2)` and `total - half` respectively — honest grand-total footer, but per-row R1 vs R2 figures are presentation stand-ins. A coach inspecting whether a player improved between R1 and R2 within a section gets a misleading 50/50 split rather than the real shape. Right fix: extend `aggregateAssessment` to return `{ section, model, r1, r2, total, max }` shape (plumb round through the inner reducer) and update the SectionBreakdown component to read the real values. Owner: Phase 12.5 polish, OR earlier if coaching feedback flags the split as misleading. Discovered: Phase 10-7, 2026-04-29.~~ Closed: Phase 12 / 12-4, 2026-04-30. `SectionTotal` extended with `r1` + `r2` fields; `aggregateAssessment` now sums per-(section, round) bucket so the values are real engine output. `buildBreakdown` in `AssessmentResults.tsx` reads `total.r1` / `total.r2` straight through. Pinned by 5 new test cases in `tests/lib/t20/score.test.ts` covering empty / single-R1 / single-R2 / mixed / cross-section invariants (r1+r2 === earned).
- [x] ~~**Results view: notes are read-only.** `t20_assessments.notes` ships from migration 007 but no `editAssessmentNotes` action exists in Phase 10's locked-9. The results view's NotesSection renders captured notes verbatim; if `notes IS NULL` the view renders an empty-state explainer. The capture wizard doesn't surface a notes input either — notes today land only via the schema column being writable through a future action OR the eventual finalize-with-notes flow. Right fix: add `editAssessmentNotes(assessment_id, notes)` action + an inline edit affordance on the results view (or fold into a "finalize with notes" step in the capture wizard's last screen). Owner: Phase 12 polish. Discovered: Phase 10-7, 2026-04-29.~~ Closed: Phase 12 / 12-4, 2026-04-30. New `editAssessmentNotes({ assessment_id, notes })` Server Action in `_actions.ts` (Zod-validated; trimmed empty string → NULL; PostgREST RLS-denial mapped to `kind='forbidden'`). NotesSection became a Client island with inline edit: "Add notes" / "Edit notes" CTA reveals a 5000-char textarea + Cancel / Save buttons; toast keyed off result kind. RLS already covers the write path (`t20_assessments_club_admin_rw` + `t20_assessments_assessor_rw` from migration 010).
- [x] ~~**Live wizard finalize fails on over-target captures + latent notes-shape + silent RLS denial.** Manual QA on a real Twenty 20 capture immediately after the 12-4 close (`1aad798`) tripped a `code: 23514` PostgREST error from the `t20_assessments_percentage_range` CHECK constraint: `total_score: 410, percentage: 128.13`. The wizard surfaced a generic "Save failed" toast with no further detail. Three latent bugs in `finalizeAssessment` + `aggregateAssessment` — only the first surfaced live; the other two were uncovered during diagnostic instrumentation. Owner: Phase 12 / 12-4 hotfix. Discovered: 2026-04-30 (manual QA against the 12-4 close).~~ Closed: Phase 12 / 12-4 hotfix, 2026-05-01 — three commits on top of `1aad798`. Root causes + fixes: **(1) Percentage clamp missing.** `aggregateAssessment` divided `earned` (raw points) by `grandMax` (320, a calibration target — not a theoretical ceiling) and produced `percentage > 100`. The DB CHECK pins `[0, 100]`. Fix in `lib/t20/score.ts`: `Math.min(100, (earned / max) * 100)`. Raw `earned` is preserved separately on `total_score` so coaches can see the over-target absolute. **(2) Notes-schema mismatch.** `finalizeSchema.notes` was `z.string().trim().max(2000).optional()` (a pre-migration-041 leftover) — any caller passing notes-as-string would have tripped the `t20_assessments_notes_shape` CHECK. No live caller did, but the type mismatch was a real latent landmine. Fix in `app/(club-admin)/manage/t20/_actions.ts`: reshape to the categorised `z.object({ strengths?, watch?, focus?, legacy? }).optional()` matching `editAssessmentNotesSchema` + migration 041's jsonb column shape. Subsequently extracted to a single shared `notesPayloadSchema` (commit `8986a9d`) so the two action paths can't drift again. **(3) Silent UPDATE-no-rows under RLS denial.** The `finalizeAssessment` UPDATE chain ran without `.select(...)`, so a silent RLS denial (UPDATE matched no rows but returned no error) made the action return `kind='ok'` and the wizard navigated to an unchanged results page. Fix: `.select("id")` chain + 0-rows guard returning `kind='error'` with a hint about club admin assignment. Same diagnostic pass also caught a related silent error in the deliveries fetch (`const { data: rows } = ...` swallowed PostgREST errors and surfaced them as `no_deliveries`); now captures `rowsErr` explicitly. Hotfix commits: `1162f1f` (TEMP instrumentation, reverted), `2312a70` (clamp + schema reshape + silent-error guards), `017a3be` (revert instrumentation), `8986a9d` (shared notesPayloadSchema), `9b667a0` (integration test), `e188503` (PHASE_LOG amendment). Pinned by +3 unit tests in `tests/lib/t20/score.test.ts` (clamp behaviour) and a new 3-case integration suite at `tests/integration/actions/t20-finalize.test.ts` exercising the live Server Action end-to-end against the test cloud Supabase stack (over-target clamp + notes-as-string rejection + cross-club RLS denial).
- [x] ~~**Results view: coach-categorised notes (Strengths / Watch / Focus) not implemented.** The design source's `t20-page-results.jsx` mocks three categorised tiles ("Strengths" / "Watch" / "Coach focus") in the assessor-notes section. Today's schema has a single `notes text` column; the categorisation is mock data. Right fix: schema split into `notes_strengths` + `notes_watch` + `notes_focus` columns OR a richer `notes jsonb` shape with `{ strengths, watch, focus }` keys; corresponding edit affordance in the wizard's finalize step or on the results view. Owner: Phase 12 schema split. Discovered: Phase 10-7, 2026-04-29.~~ Closed: Phase 12 / 12-4, 2026-05-01. Migration 041 reshapes `t20_assessments.notes` from text to jsonb with optional keys (strengths / watch / focus / legacy) pinned by `t20_assessments_notes_shape` CHECK constraint backed by IMMUTABLE helper `t20_notes_keys_valid(jsonb)`. `editAssessmentNotes` action accepts the categorised input shape; sparse-object semantics (key-absent === no notes for that category); all-empty collapses to NULL. NotesSection rewritten as 3-tile grid (one per category) with independent inline edit per tile; legacy notes render in a read-only banner when present (reserved for future imports of pre-12-4 notes). Pinned by 5 new test cases in `tests/app/club-admin/t20-results.test.tsx` covering tile rendering, populated/empty branches, edit CTA copy, edit-mode reveal, and legacy banner.
- [x] ~~**Results view: "Schedule next" button dropped.** Design source's hero action bar has both "Export PDF" and "Schedule next" — we ship Export PDF (placeholder) only. Schedule-next would book a future Twenty 20 assessment for the same player, but no future-T20 booking schema exists today (the existing `bookings` table is rink reservations, not a separate "scheduled assessments" concept). Right fix: either repurpose the bookings table with a new `purpose='t20_assessment'` enum value (low-cost — fits within existing booking RLS) OR add a dedicated `t20_scheduled_assessments` table. Owner: Phase 12.5 design fidelity polish. Discovered: Phase 10-7, 2026-04-29.~~ Closed: Phase 12 triage (`9f77b73`), 2026-04-30 — consolidated into the Scheduled-send infrastructure (deferred) entry in the Phase 11 / Phase 12 area; same dispatcher dependency.
- [x] ~~**Rubrics page: YAML upload path not supported.** Design source's upload zone description says "JSON or YAML"; v1 ships JSON-only. YAML support requires adding a parser dep (e.g. `js-yaml`) — small (~25KB gzipped) but a new dependency to justify. Most rubric authoring will happen in JSON-aware tooling anyway (the rubric is computer-generated more often than hand-typed). Owner: Phase 12.5 polish if there's actual user demand for YAML; otherwise drop the YAML mention from the description copy. Discovered: Phase 10-8, 2026-04-29.~~ Closed: Phase 12 / 12-6 audit, 2026-04-30 — already at parity (option B taken pre-12-6). Audit at 12-6: shipped copy at `app/(super-admin)/platform/rubrics/_components/RubricsClient.tsx:331` reads "JSON validated against the v1 schema before staging as a draft." — no YAML mention, JSON-only as v1 spec. Drift was open against the design source's "JSON or YAML"; the shipped code already took the "drop the YAML mention" option. No code change required.
- [x] ~~**Rubrics page: View schema modal not implemented.** Design source has a "View schema" affordance next to the upload CTA. Super-admins can read `lib/t20/rubric.ts` directly today, so the v1 surface drops the button. Right fix: a small modal showing the JSON-shape preview (ideally generated from the Zod schema's `_def` rather than hand-maintained) so a super-admin can copy-paste a starter JSON. Owner: Phase 12.5 → 12.5-3 (audit id `rubrics-view-schema-modal`). Audit-spec: shadcn `<Dialog>` (max-width 720, scrollable) showing rubric `name + version`, table with `# / Section / Model / Max R1 / Max R2 / Total` columns, footer Grand-total row, expandable `<details>` JSON reveal for power users. No copy-to-clipboard primitive at v1. Discovered: Phase 10-8, 2026-04-29.~~ Closed: Phase 12.5 / 12.5-3 (`6b9b894`), 2026-05-01. New `<RubricSchemaDialog>` component (`app/(super-admin)/platform/rubrics/_components/RubricSchemaDialog.tsx`, 156 lines) wired into the VersionsTable rows via a per-row "View schema" button (icon: lucide Code2; visible only when `r.rubric` parsed cleanly). Dialog renders the section table with the audit's exact column set (# / Section / Model / Max R1 / Max R2 / Total), tfoot Grand total row, and a `<details>` JSON reveal with the full rubric serialisation. Section labels reuse the canonical 7-section order + human labels from `AssessmentResults.tsx`; max-points calc reads through `sectionMaxes(rubric)` so the modal stays in lockstep with the live grading engine. Pinned by 6 cases in `tests/app/super-admin/rubric-schema-dialog.test.tsx`.
- [x] ~~**Rubrics page: Export changelog button disabled.** Visible in the versions table header per design source but disabled with tooltip "Changelog export ships with the PDF template follow-up". Same dependency on the PDF template work; conceptually the changelog is a CSV/PDF export of the diff history across all activated versions. Right fix: pair with the PDF template work — or ship as a JSON endpoint earlier if super-admin tooling needs it sooner. Owner: alongside the PDF template follow-up. Discovered: Phase 10-8, 2026-04-29.~~ Closed: Phase 12 triage (`9f77b73`), 2026-04-30 — consolidated into the PDF template + changelog export entry above.

### Phase 11 — Comms

- [x] ~~**Dev-only invite banner on new-club detail page.** Phase 4c surfaces the generated admin-invite URL via a sessionStorage-backed banner on `/platform/clubs/[id]` when `NODE_ENV !== 'production' && NEXT_PUBLIC_APP_ENV !== 'production'`. Dismissible, 60-min TTL. Replace with a proper Resend email flow (invite email with branded template + click-through) in Phase 11. Delete the banner component + sessionStorage key once Resend is wired. Discovered: Phase 4c, 2026-04-23.~~ Closed: Phase 11-4 (`5e7ef8c` wired InviteEmail to all three invite paths via `lib/invites/email.ts sendInviteEmail`; `a7965af` deleted `DevInviteBanner.tsx`, `DevInviteBanner.test.tsx`, `lib/dev-banner.ts`, and the mounts in `clubs/[id]/page.tsx` + `manage/members/page.tsx`. Toast surfacing of `email_status` replaces the sessionStorage stash). 2026-04-30.
- [x] ~~**`v1 blocker — Phase 11 must close.` acceptInviteAction can't handle returning users.** `lib/auth/actions.ts acceptInviteAction` calls `auth.admin.createUser` unconditionally; a second invite for an email that already has an auth.users row returns "user already registered" and the player is stuck. Surfaced by the Phase-5e dual-club E2E, which works around it via service-role membership insert + sign-out/sign-in. **Pairs with the JWT-claim entry below: until both close, the dual-club switcher works visually but the only UI path to acquire a second membership (invite acceptance) is broken.** Fix when invite emails are wired (Phase 11): detect the existing user, skip createUser, just add the membership and prompt re-sign-in. Discovered: Phase 5e, 2026-04-28.~~ Closed: Phase 11-4 (`1b9d470`). `acceptInviteAction` splits into `acceptForExistingUser` (profile lookup by email; idempotent membership / admin-assignment insert; invite revoke; audit_log row with `action='invite_accepted_existing_user'`; server-side `signOut()` + redirect to `/login?invited_to=<club>&next=<role-home>`) and `acceptForNewUser` (original happy path + audit row). Pinned by `tests/integration/auth/accept-invite-existing-user.test.ts` (2 cases) and `tests/integration/auth/accept-invite-new-user.test.ts` (2 cases). 2026-04-30.
- [x] ~~**`v1 blocker — Phase 11 must close.` JWT club_ids claim is stale until next sign-in.** `custom_access_token_hook` (migration 009) bakes club_ids into the JWT at issuance. Adding a player to a new club via any path that doesn't trigger sign-in (admin direct insert, future "claim invite for existing user" flow) leaves the existing JWT's claim outdated, which RLS policy `player_read_own_clubs` then uses to block reads of the new club's row in the layout's `clubs!inner` join. The membership exists but the second club name doesn't render until re-auth. **Pairs with the acceptInviteAction entry above; both must close together for v1 dual-club to actually function in production.** Fix when same-user invite acceptance lands (Phase 11): force `auth.refreshSession()` on the client after a successful membership add, OR have the server-side flow emit a sign-in redirect. Discovered: Phase 5e, 2026-04-28.~~ Closed: Phase 11-4 (`1b9d470`). Adopted the **server-side `signOut()` + redirect** option over `auth.refreshSession()` — the refresh-token round-trip mints a new access token but its cached `app_metadata` is from the original session's user query, while a full re-sign-in fires the `custom_access_token_hook` against the post-membership state. Net cost is one re-auth dialog; net gain is a guaranteed-correct token for every subsequent request. The login page (`app/(auth)/login/page.tsx`) reads `?invited_to=<club>` and surfaces a "you've been added to {club}" confirmation banner so the re-auth feels intentional. 2026-04-30.

### Phase 12 — Stakeholder polish

- [x] ~~**Player-side `/t20` page never built.** The player navigation has a "20/20" tab (compact form per the locked naming convention's documented exception) but `/t20` is still the Phase 10 stub. Design exists from the player-side design pass (frame 06: Silver tier card, "Book Gold Assessment" CTA). Player-side surface lets a player view their own assessment history, current grade, section breakdown, and trigger a re-assessment booking. Owner: Phase 12. Approximate scope: 1 sub-checkpoint, ~half a day. Wires existing `_data.ts` fetchers (filtered to current user's profile_id), reuses existing `components/t20` primitives (`GradePill`, `CompassHeatmap`, `HandBalanceChart`, `LengthDistributionChart`, `AssessmentCard`). Critical for the marketing landing page promise — the public `ShowcaseT20` section advertises this view. Discovered: Phase 11-6, 2026-04-30.~~ Closed: Phase 12-1 (`7beb570`) shipped read-only — hero with grade pill + tier ladder, "What is Twenty 20?" explainer, upcoming-empty-state, past-assessments list. Phase 12-1 followup (this commit) adds the action surface — "Request assessment" CTA wired to `request_t20_assessment` (migration 037), upcoming-assessments populated from `bookings` filtered by `for_profile_id = auth.uid() + purpose='t20_assessment'`, full request → admin-schedule loop end-to-end.
- [ ] **Admin general booking creation form is t20_assessment-only.** Phase 9 close shipped the Bookings tab on `/manage/overview` (calendar grid + force-cancel via migration 031); admin booking CREATION was explicitly skipped per the Phase 9 close drift entry ("admins use existing `bookings_club_admin_rw` INSERT permission"). 12-1 followup added `/manage/bookings/new` to support the request → schedule loop, but the form is locked to `purpose='t20_assessment'` for v1. General-purpose admin booking creation (roll-up / practice / coaching / match / social) still has no UI — admins must insert directly via the DB or wait for the form to be extended. Right fix: extend `T20BookingForm` into a discriminated `<BookingForm purpose=...>` component with conditional fields per purpose (party_size for social, match_id for match, etc.). Owner: Phase 12.5 (UI-only; infrastructure is already in place). Discovered: Phase 12-1 followup, 2026-04-30.
- [x] ~~**Resend invite button missing in admin UI.** When `createInvite` or `createPlayerInvitesBatch` returns `email_status='failed'` (e.g., domain not yet verified, transient Resend outage) or `email_status='skipped'` (recipient opted out), there's no UI affordance for the admin to retry the email. Workaround: re-enter the invite (idempotent at the row level via `create_player_invites_batch` RPC dedupe; for `createInvite` a duplicate insert errors). Owner: Phase 12. Cheap to add — wire a "Resend invite email" button to existing rows in members table + `/platform/clubs/[id]` page; calls `sendInviteEmail({ token })` directly via a new `resendInviteEmail` action. Discovered: Phase 11-6, 2026-04-30.~~ Closed: Phase 12 / 12-3, 2026-04-30. Migration 040 added `invites.email_status / email_error / email_sent_at` columns; `sendInviteEmail` persists status onto the invite row after every attempt (sent / skipped / failed). New `resendInviteEmail(token)` Server Action in `lib/invites/actions.ts` wraps `sendInviteEmail` with role-gated authorization (club_admin owns the invite's club; super_admin bypasses) and revalidates `/manage/messages`. UI surface: `<ResendInviteButton>` Client island rendered next to the invite-row Status badge in `MembersTable`; visible on `email_status` ∈ {null, 'failed', 'skipped'} (hidden on 'sent'). NOT wired to `/platform/clubs/[id]` because that surface has no invite list today (admin invites are single-shot at club creation; drift entry tracking that gap to be opened separately if needed). Toast keyed off the result kind: success / opted-out info / failure with error text.
- [x] ~~**Live recipient-count preview missing on `/manage/messages/new`.** `resolveAudienceCount` fetcher exists in `app/(club-admin)/manage/messages/_data.ts` but the compose form doesn't call it. Admin sees actual recipient count only on the list page after sending — the audience picker just says "all members" / "tournament entrants" / "custom · N selected" without resolving to an actual count. Owner: Phase 12. Cheap — add a server-action call (debounced ~300ms) on audience-mode change + tournament selection + custom-list change. Renders below the audience picker as `Estimated recipients: N`. Discovered: Phase 11-6, 2026-04-30.~~ Closed: Phase 12 / 12-3, 2026-04-30. New `previewAudienceCount` Server Action wraps the existing `resolveAudienceCount` fetcher. New `<AudienceCountPreview>` Client island below the picker debounces audience changes (300ms via setTimeout in useEffect), fires the action via `useTransition`, and renders the count below the picker. Loading state shows `…`; null result (auth/no-club) shows `—`; zero count tinted danger to flag empty-audience misconfigs.
- [x] ~~**Notification bell role-leak.** Top-bar `NotificationsBell` (Phase 11 chrome) shows player-targeted notifications when signed in as `club_admin`, and clicking the bell routes to `/me/inbox` (player surface) regardless of role. Two distinct bugs in the same component: (a) the data fetch likely filters by `profile_id = auth.uid()` without scoping by role-appropriate notification kinds — so an admin's bell shows player rows that happened to land on their profile (e.g. own-account assessment notifications); (b) the click destination is hard-coded `/me/inbox` rather than role-routed (admin should land on an admin notifications surface). Right fix: bell fetches role-scoped notifications + routes to admin notifications surface for admins (likely `/manage/inbox` or `/manage/notifications` — confirm surface exists or create as part of 12-3). Pre-dates Phase 12; surfaced during 12-1 followup QA. Owner: Phase 12 sub-checkpoint 12-3 (Messaging admin polish). Discovered: Phase 12-1 followup QA, 2026-04-30.~~ Closed: Phase 12 / 12-3, 2026-04-30. **Bug (b) — destination routing — is fully fixed.** `NotificationsBell` now takes a `role: BellRole` prop; layouts pass `role="player"` (player layout) and `role="club_admin"` (admin layout). Pure helpers `resolveRelatedHref(role, notification)` and `viewAllHref(role)` extracted + exported for unit coverage; matrix pinned by 14 cases in `tests/components/nav/NotificationsBell.test.tsx`. Footer "View all" now branches on role. **Bug (a) — data-shape "role-leak" — re-framed:** the diagnostic confirmed the data fetch correctly scopes by `profile_id = auth.uid()`; rows are addressed to the recipient's profile. The "leak" was a destination-routing artefact (admin click landing on player surface, making admin-relevant notifications feel player-flavoured). With (b) fixed, admins land on `/manage/messages?tab=inbox` etc. and the perceived leak resolves. **Inbox/Sent split** at `/manage/messages` (URL-driven `?tab=inbox|sent`; default `inbox`) gives admins a real surface for incoming player→admin messages. Diagnostic sub-issues #1, #3, #4, #5 all closed by this commit. Sub-issue #2 (super-admin bell missing) deferred — see "Super-admin notifications bell missing" entry below.
- [x] ~~**No edit page for existing message drafts.** `/manage/messages/[id]/edit` route doesn't exist; saved drafts can't be re-edited inside the UI. Server foundation (`getMessageDetail` + `updateMessageDraft` + the action's `wrong_state` handling for non-draft rows) is already in place. Owner: Phase 12. Cheap — copy `app/(club-admin)/manage/messages/new/page.tsx` structure to `[id]/edit/page.tsx`, swap the action wire-up to call `updateMessageDraft` instead of `createMessageDraft`. Same audience picker + form sections; only the `useActionState` handler differs. Discovered: Phase 11-6, 2026-04-30.~~ Closed: Phase 12 / 12-3, 2026-04-30. New `app/(club-admin)/manage/messages/[id]/edit/page.tsx` Server Component loads the draft via `getMessageDetail`, renders the same `<ComposeForm>` with `edit={{ messageId, initial: {...} }}` props pre-populating subject / body / audience. The form's hidden `message_id` input switches `composeMessageFromForm` into the update path: when present, it calls `updateMessageDraft` (which already enforces `status='draft'`); when absent (the /new path), it calls `createMessageDraft`. wrong_state guard: page-level redirect to `/manage/messages?tab=sent&notice=wrong_state&message=<id>` if the row has transitioned out of draft. List-row "Edit draft" button surfaces on `status='draft'` rows. NOT a separate `updateMessageFromForm` action — the same `composeMessageFromForm` handles both paths via the optional `message_id` field, keeping the Client island's `useActionState` wire-up unchanged.
- [x] ~~**Remove "Send later" option from message compose form.** `/manage/messages/new` ships a "Send later" affordance writing `status='queued'` rows with `scheduled_at` timestamps, but no worker dispatches them when the timestamp elapses. Until the dispatcher (next entry below) ships, the option is a v1 lie. Remove the affordance from the form (and any `status='queued'` filter on `/manage/messages` list); messages persist as `draft` or `sent` only for v1. Split from the original Scheduled-send dispatcher entry at Phase 12 triage, 2026-04-30. Owner: Phase 12 (12-3 sub-checkpoint). Discovered: Phase 11-6, 2026-04-30.~~ Closed: Phase 12 / 12-3, 2026-04-30. ComposeForm's Section 4 (Schedule with Send-now / Send-later radios) deleted; the `ScheduleRadio` sub-component, `scheduleMode` / `scheduledAt` / `mountedNowMs` state, and `compose_action='schedule'` branch in the action all removed. Form now has 4 sections (Subject / Body / Audience / Channel) with two CTAs (Save as draft / Send now). MessagesListClient status filter chips dropped 'queued' (was 5 chips: All / Draft / Queued / Sent / Failed; now 4: All / Draft / Sent / Failed) — pre-existing 'queued' rows still render via the StatusPill colour map (back-compat) but no new ones get minted. `ComposeAction` type narrowed from `'save_draft' | 'send_now' | 'schedule'` to `'save_draft' | 'send_now'`. Existing `scheduleMessage` action retained as exported but unused; future v2 dispatcher work may reintroduce.
- [ ] **Scheduled-send dispatcher (messaging — deferred).** The messaging "Send later" button on `/manage/messages/new` was a design-source affordance that ships UI-only (the carve-out is logged in the "Remove 'Send later' option" entry above). The dispatcher itself is the post-v1 work: a worker that detects elapsed `scheduled_at` rows and calls the existing `send_message` RPC idempotently. Backend depends on hosting platform decision (Vercel cron / pg_cron / external scheduler like GitHub Actions). The `send_message` RPC is already scheduler-friendly. Acceptance: scheduled-later messages dispatch within ±60s of `scheduled_at`. Pre-condition: re-introduce the "Send later" affordance from the Phase 12 carve-out. **Note (12-1 followup, 2026-04-30):** the T20 results-view "Schedule next" button was previously consolidated here on the assumption it shared the dispatcher dependency. After 12-1 followup landed the request → schedule loop (admin-driven, not scheduler-driven), the T20 portion is satisfied independently. Phase 12.5 may add a "Schedule next" button on `/manage/t20/[id]` results that links to /manage/bookings/new pre-filled — that's a UI affordance only, no infrastructure dependency. Owner: post-v1 (messaging dispatcher only). Discovered: Phase 11-6, 2026-04-30.
- [x] ~~**PlayerBottomNav "20/20" compact-form exception.** `components/nav/PlayerBottomNav.tsx` line 40 uses the compact label `"20/20"` instead of canonical `"Twenty 20"` (per locked naming convention) due to the 76px tab-width constraint of the 5-tab bottom nav. Documented inline above the label and pinned by `tests/components/player/PlayerBottomNav.test.tsx`. Codified in the `bsa-terminology` skill at `~/.claude/skills/bsa-terminology/SKILL.md`. If the bottom-nav layout changes (taller bar, fewer tabs, or different visual treatment), revisit and use canonical `"Twenty 20"`. Owner: design-pass-when-it-comes. Discovered: Phase 11-6, 2026-04-30.~~ Closed: Phase 12 triage (`9f77b73`), 2026-04-30 — reclassified as Decision (no actionable scope; trigger is a future layout change). See `## Decisions` section for the canonical entry.

- [ ] **Feeder match_no display via hash fallback in DrawTab.** `components/tournament/MatchCard.tsx` and the bracket rendering display "Winner of M##" using a hash of `slot_*_source_match_id`'s last 3 hex chars (1–99 pseudo-number) because the joined feeder match's `match_no` isn't on the row. When match-modal-from-feeder navigation lands (Phase 7d or later), replace with real `match_no` via a join on `matches`. Owning phase: Phase 7d if navigation ships there; else Phase 12. Discovered: Phase 7c-ii, 2026-04-29.
- [x] ~~**Phase 7b cross-cutting tournament migration.** Single migration + form wire that lands two Phase 7b promises currently dropped on submit (consolidated from two separate entries at Phase 12 triage, 2026-04-30):
  - **`tournament_greens` join table.** Composite PK on (tournament_id, green_id). Phase 7b's "Greens to use" picker on `/manage/tournaments/new` is currently UI-state-only — selection isn't persisted because there's nowhere to write it. Rink assignment happens at match-scheduling time today (`matches.rink_id`), but admins want to scope greens at create time so the rink fairness algorithm only ever picks from the chosen surfaces.
  - **`tournaments.fair_rink` column.** `boolean not null default true`. The Fair Rink toggle exposed by the Phase 7b creation form is UI-state-only — toggle defaults to true, helper text explains the recommendation, but the value is dropped on submit.
  Two-commit minimum per `handibowls-standards`: migration + types regen first; form wire on `/manage/tournaments/new` second. Owner: Phase 12 (12-2 sub-checkpoint). Discovered: Phase 7b, 2026-04-29.~~ Closed: Phase 12 / 12-2, 2026-04-30. Migration 039 (`a71c350`) shipped both the `tournament_greens` join table (composite PK + RLS mirroring tournaments + indexes on both columns + ON DELETE CASCADE on tournament_id, RESTRICT on green_id) and the `tournaments.fair_rink boolean not null default true` column. Form wire ships in this same sub-checkpoint: `createTournamentSchema` accepts `fair_rink` (default true) + `green_ids` (default []); `createTournament` action persists `fair_rink` on the tournament row + fans out `green_ids` to `tournament_greens` in a follow-on insert; helper-text on the Greens picker updated from "Visual selection only for now" to "Selected greens scope which surfaces the rink-fairness algorithm picks from at match scheduling." RLS pinned by `tests/rls/tournament-greens.test.ts` (5 cases). **Edit-flow follow-up:** no tournament edit page exists today — green_ids/fair_rink edits ship when the edit page does. Drift entry "Tournament edit page missing" tracked separately under Phase 12.5.
- [x] ~~**`tournaments.fair_rink` column missing.** No column for the Fair Rink toggle exposed by the Phase 7b creation form. UI state-only. Toggle defaults to true and helper text explains the recommendation, but the value is dropped on submit. Add `fair_rink boolean not null default true` in a Phase-12 cross-cutting migration alongside the rink-fairness algorithm work. Owning phase: Phase 12. Discovered: Phase 7b, 2026-04-29.~~ Closed: Phase 12 triage (`9f77b73`), 2026-04-30 — consolidated into the Phase 7b cross-cutting tournament migration entry above.
- [x] ~~**Tournament drafts: remove disabled Save-as-draft button.** Phase 7b's "Save as draft" button on `/manage/tournaments/new` is rendered disabled with a tooltip pointing here. The `tournament_status` enum already has `'draft'`, but no action wiring exists. **User call at Phase 12 triage, 2026-04-30:** remove the disabled button (don't wire `saveTournamentDraft`). The disabled-CTA pattern is a v1 lie; tournament drafts can come back in a post-v1 phase if admin demand surfaces. Owner: Phase 12 (12-2 sub-checkpoint). Discovered: Phase 7b, 2026-04-29; reworded Phase 12 triage, 2026-04-30.~~ Closed: Phase 12 / 12-2, 2026-04-30. Disabled button removed from `app/(club-admin)/manage/tournaments/new/_components/NewTournamentForm.tsx`; the existing test case "save-as-draft button is disabled with the Phase-12 tooltip" rewritten to assert the button is REMOVED. `saveTournamentDraft` action not wired; `tournament_status='draft'` enum value retained on disk for any future post-v1 reintroduction.
- [ ] **Post-v1 feature gaps surfaced by 12-7 phase-string sweep (consolidated).** Four user-visible carve-outs whose stale "ships in Phase N" copy got replaced with neutral "coming soon" wording at 12-7. None blocked Phase 12 close; each surfaced a real feature gap that needs an owner phase. Consolidated rather than four separate entries because they share the post-v1 carve-out shape. Discovered: Phase 12 / 12-7, 2026-04-30. Owner: post-v1 unless escalated.
  - **/me settings deep-links not wired.** Profile / Notifications / Wet hands default / Account rows are placeholder list items with em-dash trailing chips. Pre-12-7 footer copy referenced "Phase 11 comms wiring"; replaced with "More settings coming soon." Real fix: each row needs a destination + per-section page (e.g. `/me/notifications`, `/me/wet-hands`). Owner: post-v1.
  - **CommsTab tournament-scoped Send disabled.** `app/(club-admin)/manage/tournaments/[id]/_components/tabs/CommsTab.tsx` Send button ships disabled (real feature gap; the tab UI exists but the action wire to `send_message` scoped to the tournament's entrants doesn't). Pre-12-7 tooltip referenced "Phase 11"; replaced with neutral "Tournament-scoped comms integration coming soon." Real fix: thread `tournament_id` audience selector through the existing `send_message` RPC + `composeMessageFromForm` action. Owner: post-v1 (the global compose form on `/manage/messages/new` covers the same use case via the audience picker today).
  - **Round-robin / sectional standings tables.** `app/(player)/(gated)/tournaments/[id]/page.tsx` standings section is a "coming soon" placeholder. Pre-12-7 copy referenced Phase 12; replaced with "Standings will be available once the tournament is in progress." Real fix: standings data layer + table per format (round-robin needs win/loss/shots-up rollup; sectional needs section grouping + advancement count). Owner: post-v1, gated on round-robin engine work.
  - **Super-admin /platform/tournaments view.** Stub page with neutral "Coming soon." (was "Coming in Phase 7."). Real fix: a super-admin tournament browse / audit view across all clubs; would reuse `TournamentsList` with cross-club RLS + an extra column for host club. Owner: post-v1.
- [x] ~~**List search filters paginated subset, not full dataset.** Reproduced as super_admin on `/platform/clubs`: search input only matches rows on the currently-active page, not the full clubs list. If clubs span 3 pages, search returns matches from page 1 only. Likely class-of-bug across all paginated TanStack Table surfaces — search needs to either (a) query the full dataset server-side on input change, or (b) fetch all rows once and filter client-side, or (c) keep server pagination and shift search to a separate server-side query that bypasses pagination. Audit every paginated list surface (admin clubs, members, tournaments, messages, t20 assessments, super-admin clubs / districts / etc) and confirm the fix covers all of them. Owner: Phase 12 12-7 close (folds into pre-stakeholder QA audit). Discovered: Phase 12 12-6 in-flight, 2026-04-30.~~ Closed: Phase 12 / 12-7 (`825823a`), 2026-04-30. Audit confirmed **single-surface** scope, not class-of-bug: `/platform/users` already threads `q` correctly via `listUsers`; `MembersTable`, `DistrictsTable`, `TournamentsList`, and the `/manage/messages` + `/manage/t20` lists all fetch the full set without server pagination, so client-side `globalFilter` already sees everything. Only `/platform/clubs` had the bug. Fix: option (a) per the entry's framing — server-side `q` thread on `listClubs` mirroring `listUsers`'s pattern, plus a new `ClubsSearchBar` URL-pushing input replacing the in-table `globalFilter`. ClubsTable now renders the already-filtered server-rendered subset; pagination Previous/Next preserve `q`. Pinned by 6 cases in `ClubsTable.test.tsx` (no-client-filter assertion + q-aware empty-state) + 3 cases in new `ClubsSearch.test.tsx` (debounce / clear / typing-coalesce — pattern lifted from UsersSearch.test).
- [ ] **[BLOCKED: coach input pending] BSA seeding algorithm details unverified.** Three-method framing (random / seeded / sectional, encoded in `tournaments.seeding_method` enum + `lib/tournaments/seeding.ts`) is confirmed at the structural level against BSA Domestic Regulations + Standard Procedures + Port Natal Conditions of Play + World Bowls Laws Decisions. Algorithm-specific details — whether handicap balancing is canonical, whether districts have local variations, whether sectional advancement counts (top M) are nationally fixed — were not surfaced in public-source research and should be verified with a BSA-accredited coach before being treated as canonical. **Phase 12 close does not wait on this.** The moment a BSA-accredited coach is available for review, lands in whichever sub-checkpoint is open at the time. Pre-condition for any future seeding-algorithm change. Owning phase: Phase 12 (PARKED — stakeholder-input polish; flag updated at Phase 12 triage, 2026-04-30). Discovered: Phase 6a, 2026-04-29.

### Phase 13 — Technical polish

- [ ] **Verification gates miss live-wizard end-to-end paths.** Phase 12 / 12-4 close (`1aad798`) passed all five gates — tsc clean / lint 0 errors / 1203 unit / 111 integration / build green — yet the live wizard → `finalizeAssessment` → SQL UPDATE path was never exercised by any test. The unit suite mocks the action; the RLS integration suite (`tests/rls/t20.test.ts`) doesn't exercise post-208-delivery finalize. Real bug surfaced via manual QA the same day: `aggregateAssessment` produced `percentage = 128.13` (over 100) on a high-scoring capture, tripping the `t20_assessments_percentage_range` CHECK constraint at finalize. The percentage clamp + finalizeSchema notes-shape alignment shipped as `12-4 hotfix` on top of `1aad798`; the gap in coverage that let it through is the Phase 13 audit task. **Phase 13 audit task:** identify every Server Action that has no real-database integration test exercising the full call shape; prioritise the high-blast-radius ones (`finalizeAssessment`, `admin_schedule_t20_assessment`, `send_message`, `acceptInvite`); add at least one RLS-mode integration case per action covering the happy path against a realistic-scale fixture (e.g. finalize against a 200+ delivery seed). Owner: Phase 13. Discovered: Phase 12 / 12-4 hotfix, 2026-05-01. **Partial closure 2026-05-01:** `finalizeAssessment` now has a 3-case integration suite at `tests/integration/actions/t20-finalize.test.ts` covering the over-target clamp, notes-shape rejection, and cross-club RLS denial. Pattern (vi.mock of `next/headers` + `next/cache` + `@/lib/auth/role` + `@/lib/supabase/server` to inject a JWT-bound supabase-js client and a per-test AuthContext) is reusable for the remaining three actions. Phase 13 narrows to backfilling `admin_schedule_t20_assessment`, `send_message`, and `acceptInvite` against the same template (the RPC actions already have RLS-mode coverage at `tests/rpc/`; the gap is the action wrappers).
- [ ] **tz-naive date formatters across pre-Phase-7 surfaces.** Five sites use `new Date(s).toLocaleDateString(...)` without an explicit `timeZone` — that's the browser's local zone, not Africa/Johannesburg per project standards. Phase 7b extracted `lib/format/dates.ts` with `formatDateZA` etc. backed by `Intl.DateTimeFormat({ timeZone: 'Africa/Johannesburg' })`; Phase 7's surfaces (TournamentCard, TournamentsList, and from 7c-i onward) already go through the helper. Pre-Phase-7 sites carry the bug: `app/(player)/(gated)/me/_components/ClubMembershipList.tsx:72`, `app/(super-admin)/platform/clubs/[id]/_components/MembersTab.tsx:101`, `app/(super-admin)/platform/clubs/[id]/_components/TournamentsTab.tsx:45`, `app/(super-admin)/platform/clubs/[id]/_components/AdminsTab.tsx:41`, `app/(club-admin)/manage/members/_components/MembersTable.tsx:50`. Replace with helpers from `lib/format/dates.ts` at Phase 13. Owning phase: Phase 13. Discovered: Phase 7b, 2026-04-29.
- [x] ~~**AdminSidebar duplication across branches.** Phase 7's version is canonical at Phase 13 reconcile. Phase 7 preserved the splatter accent from Phase 4 design integration's version with theme-token-driven colour (Phase 4 hardcoded `preset="atomic-red"`; Phase 7 wires `identity.decorPreset` so the splatter tracks the active club preset for club_admin variant and falls back to `"atomic-red"` for super_admin). No reconcile work expected — Phase 4's branch is parked at `df7a384` and its sidebar is logically a subset of Phase 7's. Owning phase: Phase 13. Discovered: Phase 7-prep, 2026-04-29.~~ Closed: Phase 12 triage (`9f77b73`), 2026-04-30 — reclassified as Decision (entry text says "No reconcile work expected"; documentation, not open work). See `## Decisions` section for the canonical entry.
- [ ] **§18 documentation drift — `bracket.ts` content vs §18 mapping.** HANDIBOWLS_REBUILD_PLAN.md §18 line 1026 maps `lib/tournaments/bracket.ts` → `lib/tournaments/brackets/knockout.ts` ("Moved; same exports re-exported"). On disk `bracket.ts` is label helpers (`largestPowerOfTwoLE`, `roundLabel`, `finishPlacementLabel`), not knockout pairing. Phase 6a kept `bracket.ts` in place (renaming would mislabel content) and wrote a fresh `lib/tournaments/brackets/knockout.ts` for the round-1 pairing primitive — preserving §18's INTENT (knockout primitive exists) while diverging from §18's letter (bracket.ts source). Update §18 line 1026 + §9 step 1/2 to reflect on-disk reality at the Phase-13 plan reconciliation. Discovered: Phase 6a, 2026-04-29.
- [ ] **Vitest Windows worker-pool flake.** `npm test` pinned to `--fileParallelism=false` via Phase 4 prep Commit C. Upstream vitest 4.1.4 + Windows issue. File: `package.json`. Owner: Phase 13. Acceptance: remove `--fileParallelism=false` and confirm green on Windows + Linux 3 runs each. Discovered: Phase 4 prep, 2026-04-23.
- [x] ~~**RLS test club teardown.** `tests/rls/helpers.ts` seeds `test-%` clubs without cleanup. Phase 4 prep did a one-time manual wipe. Add `afterAll` teardown. Discovered: Phase 4 prep, 2026-04-23.~~ Closed Phase 8g triage, 2026-04-29. `cleanup(userIds, clubIds)` helper exists at `tests/rls/helpers.ts:145`; every RLS suite (`clubs`, `jwt-hook`, `bookings`, `profiles`, `messages`, `matches`, …) calls `afterAll(() => cleanup(...))`. The residual ON DELETE RESTRICT orphan issue is tracked separately (Phase 8e Finding 18 entry above).
- [ ] **Supabase local Storage healthcheck flake.** `npx supabase start` requires `--ignore-health-check` on Windows WSL2 due to slow service boot. Services are actually healthy; CLI window is too short. Owner: Phase 13. Acceptance: remove healthcheck retry workaround once Supabase CLI healthcheck timeout is configurable. Discovered: Phase 4 prep, 2026-04-23.
- [ ] **vitest.rls.config.ts covers non-RLS tests.** Config now runs both tests/rls/andtests/rpc/. File name mildly misleading. Rename to vitest.integration.config.ts at Phase 13 polish (trivial rename + package.json script update). Discovered: Phase 4a, 2026-04-23.
- [ ] **TanStack Table + Virtual + React Compiler lint warnings.** `react-hooks/incompatible-library` warnings on `useReactTable()` and `useVirtualizer()` — libraries return functions the compiler can't memoise. Upstream TanStack issue; warnings are not errors and unsuppressable without disabling the rule. Files: `app/(super-admin)/platform/clubs/_components/ClubsTable.tsx`, `app/(super-admin)/platform/clubs/[id]/_components/MembersTab.tsx`, `app/(super-admin)/platform/districts/_components/DistrictsTable.tsx`, `app/(club-admin)/manage/members/_components/MembersTable.tsx`. Owner: Phase 13. Acceptance: remove warning suppressions once TanStack ships compiler-friendly API or React Compiler lint escape hatch. Discovered: Phase 4b, 2026-04-23. Extended: Phase 4d, 2026-04-25; Phase 5b, 2026-04-28.
- [ ] **Playwright prod server slow on Windows.** `next start` on Windows cold-serves `/login` POST in 30-40s and RSC prefetches in 10-15s each on first access, which forced 300s test timeout / 60s expect.toBeVisible in `e2e/theme-flip.spec.ts`. Warm requests are <1s. Likely Next 16 + Windows fetch DNS + Supabase auth round-trip layering. Revisit when either Next ships faster cold starts on Windows, or CI moves to Linux. File: `playwright.config.ts`. Owner: Phase 13. Acceptance: drop the local retry once cold-serve <5s p95 on Windows verified by 10-run sample. Discovered: Phase 4b, 2026-04-23. Phase 4d follow-up: theme-flip cold-streamed `/platform/clubs/[id]` and never hydrated `tab-theme` within 60s on ~50% of fresh-build runs; absorbed via `retries: 1` (was `0` locally, `1` on CI). Extended: Phase 4d, 2026-04-25.
- [ ] **`"use client"` constant-taint sweep.** Any `"use client"` module exporting a plain const or type consumed by a server component silently resolves to empty/placeholder in prod builds. Two known occurrences found and fixed in 4b: `components/brand/ThemeApplier.tsx` (split `THEME_PRESETS` into `theme-presets.ts`) and `app/(super-admin)/platform/clubs/[id]/_components/ClubTabs.tsx` (split type/guard into `club-tabs-types.ts`). Pattern likely exists elsewhere. Phase 13 task: grep every `"use client"` module for non-component exports, audit consumers, split pure modules where needed. Discovered: Phase 4b, 2026-04-23.
- [ ] **`tests/lib/email/unsubscribe.test.ts` base64url tamper-rejection flake.** The "rejects a token whose signature byte was flipped" case (line 89-97) generates a fresh HMAC-SHA256 token then swaps the last base64url character of the signature. base64url's last character can carry trailing bits the decoder truncates — when the random HMAC happens to land on such a character, the swap produces a string that decodes to the same bytes, and the verifier correctly accepts the unchanged signature, failing the test. 3/3 isolated re-runs passed; 1/2 full suite re-runs surfaced the flake. Pre-existing — confirmed not introduced by 12-prep (markdown-only commits at `9f77b73` + `1b20559`). Right fix: tamper a payload byte instead of the signature's last character (the payload-flip case at line 78-87 is the safer pattern), OR mask off trailing bits when constructing the swap. Owner: Phase 13. Discovered: Phase 12-prep test gate, 2026-04-30; recorded: Phase 12-1 open, 2026-04-30.
- [x] ~~**`proxy.ts` `getSession()` deprecated.** Supabase SSR v0.8 prefers `getUser()` (verifies JWT against the auth server). `getSession()` only reads cookies. Non-blocking — still works — but replace at Phase 13 polish. File: `lib/supabase/proxy.ts`. Discovered: Phase 4b, 2026-04-23.~~ **Superseded by Phase 4c.5** — `proxy.ts` intentionally uses `getSession()` + local JWT decode for routing decisions to avoid a concurrent-`getUser()` race under Next.js RSC prefetch. Authoritative auth checks (`requireRole`, RLS, server actions) continue to call `getUser()`. Superseded: 4c.5 (`d19c09d`), 2026-04-23.

### Cross-cutting

- [ ] **Super-admin notifications bell missing.** `app/(super-admin)/layout.tsx` does not mount `<NotificationsBell>`. Super-admins have no realtime notification surface; any platform-level events that target a super-admin's profile (none currently fire, but cluster-wide announcements / multi-club admin actions could in future) are invisible. Right fix: mount the bell with `role="super_admin"` (requires extending `BellRole` from the current player|club_admin union); add platform-flavoured destinations to `resolveRelatedHref` (e.g. `related_kind='club'` → `/platform/clubs/{id}`); add a `viewAllHref('super_admin')` target — likely `/platform/inbox` (new route) or repurpose `/manage/messages?tab=inbox` if super-admins typically have a host club. Owner: post-v1 / v2 — super-admin role is a small audience and no platform-level events currently fire notifications targeting super-admin profiles. Discovered: Phase 12 / 12-2 diagnostic, 2026-04-30; deferred at 12-3 open per locked user call.
- [ ] **Personal theme override (post-v1 / v2).** Current theming is club-driven via `identity.decorPreset` — every member of a club sees that club's preset. Add an optional personal override at the profile level so individual users can pick a different preset, overriding their club's default. Approximate scope: `profiles.theme_preset_override` column + settings UI on `/me` + identity resolver checks override before falling back to club preset. Theme tokens already drive all chrome so no design work — data + UI plumbing only. Multi-club admins already get per-club theming via `decorPreset` switching; this is specifically about member-level override of club default. Owner: post-v1 / v2. Discovered: Phase 12 / 12-1 followup, 2026-04-30.

---

## Decisions (documentation only — not open work)

Records audit conclusions and intentional carve-outs that are not open work but should not be lost. Entries here have no `- [ ]` / `- [x]` checkbox because they are not tasks; they are pinned context. Do not delete entries from this section — supersede with a new entry if a decision is reversed.

- **AdminSidebar foot card contract: club name (club_admin) / user name (super_admin).** Primary line shows club name when there's a single club to display; user name when there isn't. Bowl-vs-avatar branching tied to this contract. Phase 8 player surfaces will need their own contract decision (players can be at multiple clubs). **Recorded:** Phase 7 manual QA, 2026-04-29.

- **PlayerBottomNav `"20/20"` compact-form exception.** `components/nav/PlayerBottomNav.tsx:40` uses the compact label `"20/20"` instead of canonical `"Twenty 20"` (per locked naming convention) due to the 76px tab-width constraint of the 5-tab bottom nav. Pinned by `tests/components/player/PlayerBottomNav.test.tsx` and codified in the `bsa-terminology` skill. **Trigger to revisit:** if the bottom-nav layout changes (taller bar, fewer tabs, or different visual treatment), re-evaluate against canonical `"Twenty 20"`. **Recorded:** Phase 11-6, 2026-04-30.

- **AdminSidebar duplication across branches.** Phase 7's `AdminSidebar` is canonical. Phase 7 preserved the splatter accent from Phase 4 design integration's version with theme-token-driven colour (Phase 4 hardcoded `preset="atomic-red"`; Phase 7 wires `identity.decorPreset` so the splatter tracks the active club preset for `club_admin` variant and falls back to `"atomic-red"` for `super_admin`). Phase 4's branch is parked at `df7a384` and its sidebar is logically a subset of Phase 7's. **No reconcile work expected.** **Recorded:** Phase 7-prep, 2026-04-29.

---

## Closed items (audit trail)

- [x] ~~**Q11 impersonation flag removal.**~~ Closed: Phase 4 prep Commit A (`cb77dbb`), 2026-04-23.
- [x] ~~**Invite preset fallback → `core-black`.**~~ Closed: Phase 4 prep Commit B (`03c6fb0`), 2026-04-23.
- [x] ~~**`InviteBackground` decorative splatter themed-by-club.**~~ Closed: Phase 4 prep Commit D (`7f8f975`), 2026-04-23.
- [x] ~~**T20 v2 rubric additions — A+ split, distance buckets, coach-validated grading, positioning copy.**~~ Scope added to Phase 10 via plan amendment. Closed: plan amendment `319e205`, 2026-04-23.
